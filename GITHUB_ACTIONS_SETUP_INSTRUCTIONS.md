# 🔧 Инструкция по Настройке GitHub Actions для Stock Tracker

## 📋 Что Вам Понадобится

Перед настройкой подготовьте следующие данные:

1. **Wildberries API Key** - Ваш API ключ от Wildberries
2. **Google Service Account JSON** - Полное содержимое файла `config/service-account.json`
3. **Google Sheet ID** - ID вашей Google таблицы (из URL)
4. **Google Sheet Name** - Название листа (обычно "Stock Tracker")

---

## 🎯 Шаг 1: Получение Данных

### 1.1 Wildberries API Key

Ваш API ключ уже должен быть в переменной окружения или в `.env` файле.

**Где найти:**
```bash
# Посмотрите в .env файле
cat .env | grep WILDBERRIES_API_KEY
```

### 1.2 Google Service Account JSON

**Получение содержимого:**
```bash
# Прочитайте файл service-account.json
cat config/service-account.json
```

⚠️ **ВАЖНО:** Скопируйте **полное** содержимое JSON файла (включая фигурные скобки).

### 1.3 Google Sheet ID

**Где найти:**
- Откройте вашу Google таблицу
- Посмотрите URL: `https://docs.google.com/spreadsheets/d/{ВАШ_SHEET_ID}/edit`
- Скопируйте часть между `/d/` и `/edit`

Или из переменной окружения:
```bash
cat .env | grep GOOGLE_SHEET_ID
```

### 1.4 Google Sheet Name

Название листа в таблице (обычно "Stock Tracker").

```bash
cat .env | grep GOOGLE_SHEET_NAME
```

---

## 🔐 Шаг 2: Добавление Секретов в GitHub

### 2.1 Перейдите в Настройки Репозитория

1. Откройте ваш репозиторий на GitHub: `https://github.com/ameba7464/Stock-Tracker-`
2. Нажмите **Settings** (вкладка справа)
3. В левом меню найдите **Secrets and variables** → **Actions**
4. Нажмите **New repository secret**

### 2.2 Добавьте Каждый Секрет

#### Секрет 1: WILDBERRIES_API_KEY

- **Name:** `WILDBERRIES_API_KEY`
- **Secret:** Ваш API ключ Wildberries
- Нажмите **Add secret**

#### Секрет 2: GOOGLE_SERVICE_ACCOUNT

- **Name:** `GOOGLE_SERVICE_ACCOUNT`
- **Secret:** Полное содержимое файла `config/service-account.json`
  ```json
  {
    "type": "service_account",
    "project_id": "your-project",
    "private_key_id": "...",
    "private_key": "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n",
    "client_email": "...",
    ...
  }
  ```
- Нажмите **Add secret**

⚠️ **КРИТИЧНО:** Скопируйте JSON полностью, включая все переносы строк в `private_key`.

#### Секрет 3: GOOGLE_SHEET_ID

- **Name:** `GOOGLE_SHEET_ID`
- **Secret:** ID вашей Google таблицы (например: `1abc123def456ghi789jkl`)
- Нажмите **Add secret**

#### Секрет 4: GOOGLE_SHEET_NAME (опционально)

- **Name:** `GOOGLE_SHEET_NAME`
- **Secret:** `Stock Tracker` (или ваше название листа)
- Нажмите **Add secret**

---

## ✅ Шаг 3: Проверка Workflow Файла

Workflow файл уже создан: `.github/workflows/update-stocks.yml`

**Проверьте его наличие:**
```bash
ls -la .github/workflows/update-stocks.yml
```

Содержимое должно быть таким (уже настроено):
```yaml
name: Update Stock Tracker Daily

on:
  schedule:
    - cron: '1 21 * * *'  # 00:01 МСК ежедневно
  workflow_dispatch:      # Ручной запуск

jobs:
  update-stocks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: python update_table_fixed.py
        env:
          WILDBERRIES_API_KEY: ${{ secrets.WILDBERRIES_API_KEY }}
          GOOGLE_SERVICE_ACCOUNT_KEY_PATH: ./config/service-account.json
          GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
          GOOGLE_SHEET_NAME: ${{ secrets.GOOGLE_SHEET_NAME }}
```

---

## 🧪 Шаг 4: Тестовый Запуск

### 4.1 Коммит и Push (если ещё не сделали)

```bash
git add .github/workflows/update-stocks.yml
git add update_table_fixed.py
git add requirements.txt
git commit -m "Add GitHub Actions workflow for daily stock updates"
git push origin main
```

### 4.2 Ручной Запуск для Теста

1. Откройте GitHub репозиторий
2. Перейдите на вкладку **Actions**
3. Выберите workflow **Update Stock Tracker Daily**
4. Нажмите **Run workflow** → **Run workflow**
5. Дождитесь выполнения (обычно 2-5 минут)

### 4.3 Проверка Результатов

- ✅ **Зелёная галочка** = Успешное выполнение
- ❌ **Красный крестик** = Ошибка (проверьте логи)

**Просмотр логов:**
1. Нажмите на запуск workflow
2. Нажмите на job **update-stocks**
3. Разверните каждый step для просмотра деталей

---

## 🤖 Запросы для GitHub Copilot

Если вам нужна помощь, используйте эти точные запросы:

### Запрос 1: Настройка Секретов
```
@workspace Покажи мне пошагово как добавить GitHub Secrets для моего workflow:
1. WILDBERRIES_API_KEY
2. GOOGLE_SERVICE_ACCOUNT (содержимое JSON файла)
3. GOOGLE_SHEET_ID
4. GOOGLE_SHEET_NAME

Workflow файл: .github/workflows/update-stocks.yml
```

### Запрос 2: Отладка Workflow
```
@workspace Мой GitHub Actions workflow не работает. 
Файл: .github/workflows/update-stocks.yml
Ошибка: [вставьте сообщение об ошибке из логов]

Помоги найти и исправить проблему.
```

### Запрос 3: Проверка Конфигурации
```
@workspace Проверь корректность моей GitHub Actions конфигурации:
- Workflow файл: .github/workflows/update-stocks.yml
- Основной скрипт: update_table_fixed.py
- Зависимости: requirements.txt

Все ли секреты правильно используются?
```

### Запрос 4: Настройка Расписания
```
@workspace Как изменить расписание запуска GitHub Actions workflow?
Текущее: 00:01 МСК ежедневно
Нужно: [укажите новое время]

Файл: .github/workflows/update-stocks.yml
```

### Запрос 5: Добавление Уведомлений
```
@workspace Добавь уведомления в GitHub Actions workflow при успехе/ошибке:
- Slack webhook: [URL]
- Email: [адрес]

Файл: .github/workflows/update-stocks.yml
```

---

## 🔍 Частые Проблемы и Решения

### Проблема 1: "Secret not found"

**Причина:** Секрет не добавлен или имеет неправильное имя.

**Решение:**
1. Проверьте названия секретов (регистр важен!)
2. Убедитесь, что все 4 секрета добавлены
3. Названия должны точно совпадать с workflow файлом

---

### Проблема 2: "Invalid service account JSON"

**Причина:** JSON файл скопирован не полностью или с ошибками.

**Решение:**
1. Откройте `config/service-account.json`
2. Скопируйте **весь** файл целиком
3. Убедитесь, что `private_key` включает все переносы строк `\n`
4. Добавьте секрет заново

---

### Проблема 3: "Permission denied - Google Sheets"

**Причина:** Service account не имеет доступа к таблице.

**Решение:**
1. Откройте вашу Google таблицу
2. Нажмите **Share** (Поделиться)
3. Добавьте email из service account JSON (`client_email`)
4. Дайте права **Editor** (Редактор)

---

### Проблема 4: "Module not found"

**Причина:** Отсутствуют зависимости в `requirements.txt`.

**Решение:**
1. Проверьте, что `requirements.txt` есть в репозитории
2. Убедитесь, что в workflow есть шаг установки:
   ```yaml
   - run: pip install -r requirements.txt
   ```

---

### Проблема 5: Workflow не запускается по расписанию

**Причина:** GitHub Actions требует активности в репозитории.

**Решение:**
1. Сделайте первый ручной запуск
2. GitHub автоматически активирует расписание
3. Если репозиторий неактивен 60 дней, расписание отключается

---

## 📊 Мониторинг и Логи

### Просмотр Истории Запусков

1. **Actions** → **Update Stock Tracker Daily**
2. Список всех запусков с датами и статусами
3. Нажмите на любой запуск для просмотра деталей

### Анализ Логов

Каждый step в workflow показывает:
- ✅ Успешные команды (зелёный)
- ❌ Ошибки (красный)
- ⚠️ Предупреждения (жёлтый)

**Полезные команды в логах:**
```bash
# Установка зависимостей
pip install -r requirements.txt

# Создание service account файла
echo '${{ secrets.GOOGLE_SERVICE_ACCOUNT }}' > config/service-account.json

# Запуск синхронизации
python update_table_fixed.py
```

---

## 🎯 Проверка Работы

После первого успешного запуска:

1. ✅ Откройте вашу Google таблицу
2. ✅ Проверьте, что данные обновились
3. ✅ Проверьте timestamp последнего обновления
4. ✅ Убедитесь, что нет ошибок в структуре

---

## 📅 Расписание Запусков

**Текущее расписание:** Ежедневно в 00:01 МСК (21:01 UTC)

**Cron выражение:** `1 21 * * *`

### Как изменить расписание:

```yaml
schedule:
  - cron: '0 12 * * *'  # 15:00 МСК (полдень UTC)
  - cron: '0 0 * * 1'   # Каждый понедельник в 03:00 МСК
  - cron: '*/30 * * * *' # Каждые 30 минут (не рекомендуется!)
```

**Калькулятор Cron:** https://crontab.guru/

---

## 🚀 Готово!

После выполнения всех шагов:

1. ✅ Секреты добавлены в GitHub
2. ✅ Workflow файл находится в репозитории
3. ✅ Первый тестовый запуск успешен
4. ✅ Автоматическое расписание активировано

**Ваш Stock Tracker теперь работает автоматически!** 🎉

---

## 📞 Поддержка

Если возникли проблемы:

1. **Проверьте логи** в GitHub Actions
2. **Используйте запросы для Copilot** (см. выше)
3. **Запустите локальные тесты:**
   ```bash
   python test_github_actions.py
   python comprehensive_test.py
   ```

---

*Инструкция создана: 8 ноября 2025 г.*  
*Версия: 1.0*
