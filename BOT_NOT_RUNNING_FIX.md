# 🚨 Бот не работает - Инструкция по восстановлению

**Дата:** 25 декабря 2025 г.  
**Проблема:** Бот не отвечает на команды  
**Причина:** Облачный бот в Yandex Cloud не запущен

---

## 🔍 Диагностика показала:

✅ Бот существует: @WildBStockBot (ID: 8558236991)  
✅ Telegram API доступен  
✅ Webhook не установлен (polling mode)  
✅ Локальный бот не запущен (нет конфликта)  
❌ **Облачный бот не работает** - не запрашивает обновления

---

## 🔧 Шаги для восстановления:

### Вариант 1: Перезапуск через SSH (РЕКОМЕНДУЕТСЯ)

```bash
# 1. Подключиться к VM
ssh yc-user@<your-vm-ip>

# 2. Проверить статус контейнера
sudo docker ps -a | grep stock-tracker-bot

# 3. Посмотреть логи (если контейнер есть)
sudo docker logs --tail 50 stock-tracker-bot

# 4. Перезапустить контейнер
sudo docker restart stock-tracker-bot

# Если контейнер остановлен:
sudo docker start stock-tracker-bot

# Если контейнера нет или он сломан - редеплой:
# См. Вариант 2
```

### Вариант 2: Редеплой через GitHub Actions

```bash
# 1. В GitHub зайти в Actions
# 2. Выбрать "Deploy Telegram Bot"
# 3. Нажать "Run workflow" → "Run workflow"
# 4. Дождаться завершения (2-3 минуты)
```

### Вариант 3: Ручной деплой через git push

```bash
# 1. Сделать любое небольшое изменение в коде бота
cd Stock-Tracker/telegram-bot
# Например, добавить комментарий в файл

# 2. Закоммитить и запушить
git add .
git commit -m "Trigger bot redeploy"
git push origin main

# 3. GitHub Actions автоматически задеплоит
```

---

## ✅ Проверка после восстановления:

```bash
# 1. На VM проверить что контейнер работает
sudo docker ps | grep stock-tracker-bot
sudo docker logs -f stock-tracker-bot

# 2. Локально проверить статус (без запуска!)
python telegram-bot/check_bot_status.py

# 3. Написать боту /start
# Должен ответить
```

---

## 🔍 Проверка текущего статуса (без запуска бота):

```powershell
# PowerShell команды для проверки
Invoke-RestMethod -Uri "https://api.telegram.org/bot<TOKEN>/getMe"
Invoke-RestMethod -Uri "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
```

---

## 📞 Контакты для доступа к VM:

- **Host:** `<your-vm-ip>` (из GitHub Secrets: VM_HOST)
- **User:** `yc-user` (из GitHub Secrets: VM_USER)
- **SSH Key:** GitHub Secret: VM_SSH_KEY

---

## ⚠️ ВАЖНО:

**НЕ ЗАПУСКАТЬ бота локально!** Это создаст конфликт.

Используйте только:
- ✅ Перезапуск на VM
- ✅ Редеплой через GitHub Actions
- ✅ Диагностику через `check_bot_status.py`

---

**Следующий шаг:** Подключитесь к VM и перезапустите контейнер бота.
