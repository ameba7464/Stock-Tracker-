# 🧪 Руководство по Тестированию Stock Tracker

## 📋 Обзор

Проект включает 4 тестовых скрипта для проверки всех компонентов системы:

1. **`comprehensive_test.py`** - Комплексное тестирование всех компонентов
2. **`test_github_actions.py`** - Проверка GitHub Actions конфигурации
3. **`integration_test.py`** - End-to-end интеграционный тест
4. **`run_all_tests.py`** - Запуск всех тестов одной командой

---

## 🚀 Быстрый Старт

### Запуск Всех Тестов

```bash
python run_all_tests.py
```

Этот скрипт автоматически запустит все тесты и создаст отчёты:
- `all_tests_report.json` - JSON отчёт
- `TEST_RESULTS.md` - Markdown отчёт

---

## 📝 Индивидуальные Тесты

### 1. Тест GitHub Actions

Проверяет конфигурацию GitHub Actions workflow.

```bash
python test_github_actions.py
```

**Проверяет:**
- ✅ Наличие workflow файлов
- ✅ Корректность YAML синтаксиса
- ✅ Наличие всех необходимых секретов
- ✅ Структуру workflow
- ✅ Совместимость с GitHub Actions runner
- ✅ Симуляцию GitHub Actions окружения

**Результат:** `github_actions_test_report.json`

---

### 2. Комплексный Тест

Тестирует все компоненты проекта.

```bash
python comprehensive_test.py
```

**Проверяет:**
1. ✅ Переменные окружения
2. ✅ Установленные зависимости
3. ✅ Конфигурацию проекта
4. ✅ Подключение к Google Sheets
5. ✅ Wildberries API (Statistics, Marketplace, Orders)
6. ✅ Синхронизацию данных
7. ✅ Систему логирования
8. ✅ Обработку ошибок
9. ✅ Производительность

**Результат:** `test_report.json`

⚠️ **Внимание:** Этот тест выполняет реальную синхронизацию с API и может занять несколько минут.

---

### 3. Интеграционный Тест

End-to-end тестирование полного цикла синхронизации.

```bash
python integration_test.py
```

**Проверяет:**
1. ✅ Загрузку конфигурации
2. ✅ Загрузку данных из Wildberries API
3. ✅ Обработку и валидацию данных
4. ✅ Синхронизацию с Google Sheets
5. ✅ Целостность данных в таблице
6. ✅ Производительность операций

**Результат:** `integration_test_report.json`

⚠️ **Внимание:** Этот тест выполняет полную синхронизацию и изменяет данные в Google Sheets.

---

## 📊 Интерпретация Результатов

### Успешный Тест ✅

```
✅ PASSED Test Name (2.34s) - Test completed successfully
```

### Проваленный Тест ❌

```
❌ FAILED Test Name (1.23s) - Error message
   Details: Additional error information
```

### Итоговая Сводка

```
================================================================================
📊 СВОДКА РЕЗУЛЬТАТОВ ТЕСТИРОВАНИЯ
================================================================================
Всего тестов: 10
✅ Пройдено: 9
❌ Провалено: 1
📈 Процент успеха: 90.0%
⏱️  Время выполнения: 45.67s
================================================================================
```

---

## 🔍 Детальные Отчёты

### JSON Отчёты

Все тесты создают JSON отчёты с детальной информацией:

```json
{
  "summary": {
    "total": 10,
    "passed": 9,
    "failed": 1,
    "success_rate": 90.0,
    "duration": 45.67
  },
  "results": [
    {
      "name": "Test Name",
      "passed": true,
      "message": "Test completed successfully",
      "duration": 2.34,
      "details": {}
    }
  ]
}
```

### Markdown Отчёты

Удобные для чтения отчёты в формате Markdown:

- `TEST_RESULTS.md` - Краткие результаты всех тестов
- `COMPREHENSIVE_TEST_REPORT.md` - Детальный анализ всего проекта

---

## ⚙️ Настройка Переменных Окружения

Перед запуском тестов убедитесь, что настроены все необходимые переменные:

```bash
# Wildberries API
WILDBERRIES_API_KEY=your_api_key_here

# Google Sheets
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=./config/service-account.json
GOOGLE_SHEET_ID=your_sheet_id_here
GOOGLE_SHEET_NAME=Stock Tracker

# Опционально
LOG_LEVEL=INFO
```

**Для Windows (PowerShell):**
```powershell
$env:WILDBERRIES_API_KEY = "your_api_key"
$env:GOOGLE_SHEET_ID = "your_sheet_id"
```

**Для Linux/Mac:**
```bash
export WILDBERRIES_API_KEY="your_api_key"
export GOOGLE_SHEET_ID="your_sheet_id"
```

---

## 🐛 Troubleshooting

### Проблема: "Missing required environment variable"

**Решение:** Убедитесь, что все необходимые переменные окружения установлены.

```bash
# Проверка переменных
echo $WILDBERRIES_API_KEY
echo $GOOGLE_SHEET_ID
```

---

### Проблема: "Service account file not found"

**Решение:** Проверьте путь к файлу service account:

```bash
ls -la config/service-account.json
```

Убедитесь, что файл существует и имеет корректные права доступа.

---

### Проблема: "API rate limit exceeded"

**Решение:** Подождите несколько минут перед повторным запуском теста. Wildberries API имеет лимиты на количество запросов.

---

### Проблема: "Google Sheets permission denied"

**Решение:** Убедитесь, что:
1. Service account имеет доступ к таблице
2. Email service account добавлен в разрешения таблицы
3. Таблица существует и доступна

---

## 📈 CI/CD Интеграция

### GitHub Actions

Тесты автоматически запускаются в GitHub Actions при каждом push:

```yaml
- name: Run tests
  run: |
    python test_github_actions.py
```

### Локальное Тестирование Перед Commit

Рекомендуется запускать тесты перед каждым commit:

```bash
# Быстрая проверка
python test_github_actions.py

# Полная проверка (занимает больше времени)
python run_all_tests.py
```

---

## 🎯 Рекомендации

### Перед Deployment

1. ✅ Запустите `python test_github_actions.py`
2. ✅ Проверьте, что все секреты настроены в GitHub
3. ✅ Убедитесь, что workflow файл корректен

### Регулярное Тестирование

- **Ежедневно:** Проверяйте логи GitHub Actions
- **Еженедельно:** Запускайте `comprehensive_test.py`
- **Ежемесячно:** Полный `integration_test.py`

### При Изменении Кода

1. ✅ Запустите соответствующие тесты
2. ✅ Проверьте, что все тесты проходят
3. ✅ Обновите документацию при необходимости

---

## 📚 Дополнительные Ресурсы

- **Основная документация:** `README.md`
- **Детальный отчёт:** `COMPREHENSIVE_TEST_REPORT.md`
- **GitHub Actions гайд:** `GITHUB_ACTIONS_README.md`
- **Changelog:** `CHANGELOG.md`

---

## ❓ FAQ

**Q: Как часто нужно запускать тесты?**  
A: `test_github_actions.py` - перед каждым commit. Другие тесты - по необходимости.

**Q: Тесты занимают слишком много времени. Что делать?**  
A: Используйте `test_github_actions.py` для быстрой проверки. Полные тесты запускайте реже.

**Q: Можно ли запускать тесты в production окружении?**  
A: Не рекомендуется. Используйте отдельное тестовое окружение.

**Q: Что делать, если тест "YAML синтаксис" показывает ошибку с ключом 'on'?**  
A: Это ложная ошибка из-за особенности YAML парсера. GitHub Actions корректно обрабатывает этот workflow.

---

**Успешного тестирования! 🚀**

*Обновлено: 8 ноября 2025 г.*
