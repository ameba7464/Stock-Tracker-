# Быстрая инструкция по применению изменений

## ✅ Что было сделано

### 1. Оборачиваемость как целое число
- Изменен формат отображения с `214,000` на `214`
- Обновлен файл `src/stock_tracker/database/structure.py`

### 2. Условное форматирование
- Добавлено автоматическое окрашивание в красный цвет
- Применяется к значениям оборачиваемости ≤14 дней
- Красный фон + белый жирный текст для критических товаров

## 🚀 Как применить изменения

### Вариант 1: Автоматически (при следующей синхронизации)
```bash
python main.py
```
Форматирование применится автоматически при синхронизации данных.

### Вариант 2: Вручную (только форматирование, без синхронизации)
```bash
python -c "
from src.stock_tracker.database.sheets import create_sheets_client
from src.stock_tracker.database.structure import create_table_structure

# Подключиться к таблице
client = create_sheets_client()
structure = create_table_structure(client)

# Получить worksheet
worksheet = client._get_worksheet()

# Применить форматирование
structure.apply_complete_formatting(worksheet)

print('✅ Форматирование применено успешно!')
"
```

## 🧪 Проверка результата

1. **Запустите тест:**
   ```bash
   python test_turnover_formatting.py
   ```

2. **Откройте Google Sheets таблицу**

3. **Проверьте колонку E (Оборачиваемость):**
   - ✅ Значения отображаются как целые числа (214, 30, 12)
   - ✅ Значения ≤14 окрашены в красный цвет
   - ✅ Текст в красных ячейках белый и жирный

## 📊 Примеры

### До изменений:
```
214,000  ← много нулей и запятая
12,000   ← не выделено (критический товар!)
8,000    ← не выделено (критический товар!)
```

### После изменений:
```
214      ← чистое целое число
12 🔴    ← красный фон (критический товар!)
8  🔴    ← красный фон (критический товар!)
```

## 🔍 Что означает красный цвет?

**Красный цвет = КРИТИЧЕСКИЙ УРОВЕНЬ!**

Если оборачиваемость ≤14 дней, это означает:
- Товар закончится через 2 недели или раньше
- Требуется срочная закупка
- Риск отсутствия товара на складе

## 📝 Техническая информация

**Измененные файлы:**
- `src/stock_tracker/database/structure.py` (основной файл)
  - Обновлен формат колонки оборачиваемости
  - Добавлена функция `apply_turnover_conditional_formatting()`
  - Интегрировано в `apply_complete_formatting()`

**Новые файлы:**
- `TURNOVER_FORMATTING_UPDATE.md` (полная документация)
- `test_turnover_formatting.py` (тестовый скрипт)
- `QUICK_SETUP.md` (эта инструкция)

## ❓ Устранение проблем

### Форматирование не применяется
1. Проверьте, что изменения в `structure.py` сохранены
2. Запустите тест: `python test_turnover_formatting.py`
3. Перезапустите синхронизацию

### Красный цвет не отображается
- Условное форматирование применяется при создании/обновлении таблицы
- Попробуйте вручную применить форматирование (см. Вариант 2 выше)
- Проверьте права доступа к Google Sheets API

### Значения все еще с дробной частью
- Очистите кэш: удалите `.pyc` файлы
- Перезапустите Python
- Проверьте, что используется правильный виртуальное окружение

## 📞 Поддержка

Если возникли проблемы:
1. Запустите тест: `python test_turnover_formatting.py`
2. Проверьте логи синхронизации
3. Убедитесь, что изменения в `structure.py` сохранены

## ✅ Контрольный список

- [ ] Тесты пройдены успешно
- [ ] Синхронизация выполнена
- [ ] Таблица открыта в браузере
- [ ] Оборачиваемость отображается как целое число
- [ ] Критические значения (≤14) окрашены в красный
- [ ] Текст в красных ячейках читаемый (белый, жирный)

---

**Дата обновления:** 30 октября 2025 г.  
**Статус:** ✅ Готово к использованию
