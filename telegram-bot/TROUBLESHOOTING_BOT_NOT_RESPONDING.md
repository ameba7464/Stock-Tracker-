# 🐛 Troubleshooting: Бот не отвечает на команды

## ❌ Проблема

**Симптомы:**
- Бот присылает алерты (от GitHub Actions/Alertmanager)
- Бот НЕ отвечает на команды пользователей (/start, /help и другие)
- В логах видно "User started bot", но нет ответа

**Дата обнаружения:** 25 декабря 2025 г.

---

## 🔍 Диагностика

### Проверка статуса бота

Используйте скрипт для проверки:

```bash
cd telegram-bot
python check_bot_status.py
```

**Вывод скрипта:**
```
✅ Бот найден: @WildBStockBot (ID: 8558236991)
✅ Команды зарегистрированы (2): /start, /help
✅ Webhook не установлен (polling mode)
❌ Ошибка получения обновлений: 
   Conflict: terminated by other getUpdates request
```

### 🎯 Найденная проблема

**Два экземпляра бота работают одновременно:**

1. **Облачный бот** (Yandex Cloud) - production
   - Запущен через Docker
   - Токен из GitHub Secrets
   - Работает 24/7

2. **Локальный бот** (разработка) - был запущен для теста
   - Тот же BOT_TOKEN
   - Запущен вручную

**Конфликт:**
- Telegram API позволяет только ОДНОМУ экземпляру получать обновления
- При запуске второго экземпляра происходит conflict
- Обновления распределяются между ботами случайным образом
- Результат: ни один бот не обрабатывает все команды

---

## ✅ Решение

### 1. Остановить локальный бот

**Вариант A: Найти и остановить процесс**

```powershell
# Найти процессы Python
Get-Process python | Select-Object Id, Path, StartTime

# Остановить конкретный процесс
Stop-Process -Id <process_id> -Force
```

**Вариант B: Закрыть терминал**

Просто закройте терминал, где запущен бот.

### 2. Проверить, что бот остановлен

```bash
python check_bot_status.py
```

Должно быть:
```
✅ Проверка получения обновлений...
✅ Бот может получать обновления
```

### 3. Проверить облачный бот

**На VM (Yandex Cloud):**

```bash
# SSH на сервер
ssh yc-user@<your-vm-ip>

# Проверить контейнер
sudo docker ps | grep stock-tracker-bot

# Посмотреть логи
sudo docker logs -f stock-tracker-bot

# Перезапустить если нужно
sudo docker restart stock-tracker-bot
```

---

## 📋 Правила работы с ботом

### ✅ Правильно

1. **Production:** Только ОДИН бот в облаке
   - Запускается через Docker в Yandex Cloud
   - Автодеплой через GitHub Actions
   - Работает 24/7

2. **Разработка:** Используйте ТЕСТОВЫЙ токен
   - Создайте отдельного бота через @BotFather
   - Используйте другой BOT_TOKEN в `.env.local`
   - Никогда не запускайте с production токеном

### ❌ Неправильно

- ❌ Запускать два экземпляра с одним токеном
- ❌ Запускать локально с production токеном
- ❌ Тестировать на production боте

---

## 🔧 Настройка разработки

### 1. Создайте тестового бота

```
1. Напишите @BotFather
2. /newbot
3. Придумайте имя (например, "WildBStock Dev Bot")
4. Получите токен
```

### 2. Создайте `.env.local`

```bash
cd telegram-bot
cp .env .env.local
```

Измените `BOT_TOKEN` на тестовый:

```env
BOT_TOKEN=your_test_bot_token_here
DB_HOST=localhost
# ... остальные настройки
```

### 3. Запускайте с локальным конфигом

```bash
# Указываем файл с настройками
ENV_FILE=.env.local python -m app.main
```

Или измените `config.py`:

```python
model_config = SettingsConfigDict(
    env_file=os.getenv("ENV_FILE", ".env"),  # Можно переключать
    env_file_encoding="utf-8",
    case_sensitive=False
)
```

---

## 🚨 Экстренное восстановление

### Если бот совсем не отвечает:

1. **Удалите webhook (если был установлен):**

```python
from aiogram import Bot
import asyncio

async def delete_webhook():
    bot = Bot(token="YOUR_TOKEN")
    await bot.delete_webhook(drop_pending_updates=True)
    await bot.session.close()
    print("✅ Webhook удален")

asyncio.run(delete_webhook())
```

2. **Перезапустите облачный бот:**

```bash
# На VM
sudo docker restart stock-tracker-bot

# Через GitHub Actions
# 1. Зайти в Actions
# 2. Deploy Telegram Bot
# 3. Run workflow
```

3. **Проверьте логи:**

```bash
# На VM
sudo docker logs --tail 100 stock-tracker-bot

# Локально
tail -f logs/bot.log
```

---

## 📊 Чеклист для диагностики

- [ ] Проверить, запущен ли локальный бот
- [ ] Проверить, работает ли облачный бот
- [ ] Убедиться, что webhook не установлен
- [ ] Проверить, что используется правильный токен
- [ ] Посмотреть логи на наличие ошибок
- [ ] Запустить `check_bot_status.py`
- [ ] Проверить статус на VM

---

## 💡 Почему алерты работали, а команды нет?

**Алерты (от мониторинга):**
- Отправляются через **прямой HTTP API** Telegram
- Не используют механизм polling/webhook
- Работают как обычная отправка сообщений
- `POST https://api.telegram.org/bot<TOKEN>/sendMessage`

**Команды пользователей:**
- Требуют **получения обновлений** от Telegram
- Используют polling (`getUpdates`) или webhook
- Только ОДИН экземпляр может получать обновления
- При конфликте команды теряются

---

## 📚 Дополнительная информация

- [Telegram Bot API - getUpdates](https://core.telegram.org/bots/api#getupdates)
- [Webhook vs Polling](https://core.telegram.org/bots/webhooks)
- [PRODUCTION_STATUS.md](../PRODUCTION_STATUS.md) - статус системы

---

**Дата:** 25 декабря 2025 г.  
**Статус:** ✅ Решено - остановлен локальный экземпляр бота  
**Ответственный:** Telegram Bot не должен запускаться локально с production токеном
