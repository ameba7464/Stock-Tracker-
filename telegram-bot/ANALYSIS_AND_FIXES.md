# 🔍 Анализ проблем бота и решения

**Дата анализа:** 25 декабря 2025 г.  
**Статус:** Критические проблемы найдены и исправлены

---

## 🚨 Найденные проблемы

### 1. **Race Condition при регистрации пользователей** (КРИТИЧНО)

**Описание:**  
В файле `app/bot/handlers/registration.py` была критическая ошибка race condition:

```python
# ПРОБЛЕМНЫЙ КОД:
existing_user = await get_user_by_telegram_id(session, telegram_id)
if existing_user:
    # обновляем
else:
    user = await create_user(...)  # ❌ Может упасть с UniqueViolationError
```

**Что происходило:**
- Между проверкой `existing_user` и вызовом `create_user()` другой запрос мог создать пользователя
- Это приводило к ошибке `duplicate key value violates unique constraint "users_telegram_id_key"`
- Бот падал и не отвечал пользователю

**Лог ошибки:**
```
sqlalchemy.exc.IntegrityError: duplicate key value violates unique constraint "users_telegram_id_key"
DETAIL: Key (telegram_id)=(1651759646) already exists.
```

---

### 2. **Отсутствие обработки ошибок базы данных**

**Описание:**  
Middleware `DatabaseMiddleware` не обрабатывал специфические ошибки БД:
- `IntegrityError` (нарушение уникальности, внешних ключей)
- `OperationalError` (проблемы с подключением)
- `TimeoutError` (таймаут соединения)

**Последствия:**
- Бот падал при любой ошибке БД
- Пользователь не получал понятного сообщения об ошибке
- В логах не было детальной информации о типе ошибки

---

### 3. **Неоптимальная конфигурация пула соединений PostgreSQL**

**Описание:**  
В `app/database/database.py` не были настроены важные параметры пула:
- Отсутствовал `pool_pre_ping` - проверка "живых" соединений
- Не было `pool_recycle` - переподключение при длительном idle
- Малый размер пула при пиковых нагрузках

**Последствия:**
- Возможные ошибки из-за "умерших" соединений
- Проблемы при одновременной работе многих пользователей
- Нестабильная работа при длительном запуске бота

---

### 4. **Дубликаты в базе данных**

**Обнаружено:**  
В логах видна попытка создать пользователя с `telegram_id=1651759646`, который уже существует в БД.

**Причина:**  
Предыдущие запуски бота с race condition уже создали дубликаты.

---

## ✅ Внесённые исправления

### 1. Безопасная функция `get_or_create_user()`

**Файл:** `app/database/crud.py`

Добавлена атомарная функция, которая безопасно обрабатывает race condition:

```python
async def get_or_create_user(
    session: AsyncSession,
    telegram_id: int,
    name: str,
    email: str,
    phone: str
) -> tuple[User, bool]:
    """
    Получить существующего пользователя или создать нового (безопасно от race condition).
    
    Returns:
        Tuple[User, bool]: (пользователь, был_ли_создан)
    """
    # Сначала пытаемся получить
    existing_user = await get_user_by_telegram_id(session, telegram_id)
    if existing_user:
        return existing_user, False
    
    # Пытаемся создать
    try:
        user = await create_user(...)
        return user, True
    except Exception as e:
        # Обрабатываем race condition
        if "UniqueViolationError" in str(e) or "duplicate key" in str(e):
            await session.rollback()
            existing_user = await get_user_by_telegram_id(session, telegram_id)
            if existing_user:
                return existing_user, False
        raise
```

**Преимущества:**
- ✅ Безопасная работа при параллельных запросах
- ✅ Автоматический откат транзакции при ошибке
- ✅ Повторная попытка получения пользователя
- ✅ Возвращает информацию, был ли создан новый пользователь

---

### 2. Улучшенная обработка ошибок в Middleware

**Файл:** `app/bot/middlewares/db.py`

Добавлена детальная обработка разных типов ошибок БД:

```python
try:
    return await handler(event, data)
except IntegrityError as e:
    await session.rollback()
    logger.error(f"Database integrity error: {e}")
    # Отправляем пользователю понятное сообщение
    await event.answer("⚠️ Произошла ошибка. Возможно, вы уже зарегистрированы.")
    
except (OperationalError, SQLTimeoutError) as e:
    await session.rollback()
    logger.error(f"Database connection error: {e}")
    await event.answer("⚠️ Проблемы с подключением к БД. Попробуйте снова.")
    
except Exception as e:
    await session.rollback()
    logger.error(f"Unexpected error: {e}", exc_info=True)
    await event.answer("⚠️ Непредвиденная ошибка. Обратитесь в поддержку.")
```

**Преимущества:**
- ✅ Бот не падает при ошибках БД
- ✅ Пользователь получает понятное сообщение
- ✅ В логах детальная информация о типе ошибки
- ✅ Автоматический rollback транзакции

---

### 3. Оптимизация пула соединений PostgreSQL

**Файл:** `app/database/database.py`

Добавлены оптимальные настройки для production:

```python
engine_kwargs = {
    "poolclass": AsyncAdaptedQueuePool,
    "pool_size": 10,              # Постоянные соединения
    "max_overflow": 20,            # Дополнительные при пиковой нагрузке
    "pool_timeout": 30,            # Тайм-аут ожидания
    "pool_recycle": 3600,          # Переподключение каждый час
    "pool_pre_ping": True,         # Проверка соединения перед использованием
}
```

**Преимущества:**
- ✅ Автоматическая проверка "живых" соединений
- ✅ Переподключение при длительном idle
- ✅ Поддержка до 30 одновременных соединений
- ✅ Защита от connection timeout

---

### 4. Скрипт для исправления дубликатов

**Файл:** `fix_database_duplicates.py`

Создан скрипт для проверки и исправления существующих дубликатов:

```bash
# На сервере:
cd /opt/stock-tracker-bot
source venv/bin/activate
python fix_database_duplicates.py
```

**Функции:**
- ✅ Проверка наличия дубликатов
- ✅ Показ статистики БД
- ✅ Автоматическое исправление (оставляет самую новую запись)
- ✅ Повторная проверка после исправления

---

## 🚀 Инструкции по развёртыванию исправлений

### Шаг 1: Загрузка исправлений на сервер

```bash
# С локального компьютера (PowerShell)
cd "C:\Users\miros\Downloads\Stock Tracker\Stock-Tracker\telegram-bot"

# Копируем исправленные файлы
scp app/bot/handlers/registration.py yc-user@158.160.188.247:/opt/stock-tracker-bot/app/bot/handlers/
scp app/database/crud.py yc-user@158.160.188.247:/opt/stock-tracker-bot/app/database/
scp app/database/database.py yc-user@158.160.188.247:/opt/stock-tracker-bot/app/database/
scp app/bot/middlewares/db.py yc-user@158.160.188.247:/opt/stock-tracker-bot/app/bot/middlewares/
scp fix_database_duplicates.py yc-user@158.160.188.247:/opt/stock-tracker-bot/
```

### Шаг 2: Исправление дубликатов в БД

```bash
# Подключаемся к серверу
ssh yc-user@158.160.188.247

# Переходим в папку бота
cd /opt/stock-tracker-bot

# Активируем виртуальное окружение
source venv/bin/activate

# Запускаем скрипт исправления
python fix_database_duplicates.py

# Подтверждаем исправление дубликатов
# Вводим: yes
```

### Шаг 3: Перезапуск бота

```bash
# Перезапускаем сервис бота
sudo systemctl restart stock-tracker-bot.service

# Проверяем статус
sudo systemctl status stock-tracker-bot.service

# Смотрим логи
sudo journalctl -u stock-tracker-bot.service -f
```

### Шаг 4: Проверка работы

```bash
# Проверяем последние логи
tail -f /opt/stock-tracker-bot/logs/bot.log

# Тестируем регистрацию через Telegram
# Отправьте боту: /start
```

---

## 📊 Ожидаемые результаты

После применения исправлений:

✅ **Регистрация стабильна**
- Нет ошибок `UniqueViolationError`
- Повторная регистрация обновляет данные
- Параллельные запросы обрабатываются корректно

✅ **Бот не падает**
- Все ошибки БД перехватываются
- Пользователь получает понятные сообщения
- Транзакции откатываются автоматически

✅ **Стабильное подключение к БД**
- Автоматическая проверка соединений
- Переподключение при необходимости
- Поддержка пиковых нагрузок

✅ **Чистая база данных**
- Нет дубликатов
- Уникальность telegram_id гарантирована
- Корректная статистика пользователей

---

## 🔧 Дополнительные рекомендации

### 1. Мониторинг логов

Следите за этими паттернами в логах:

```bash
# Ошибки БД
grep -i "error\|exception" /opt/stock-tracker-bot/logs/bot.log | tail -20

# Race condition warnings
grep -i "race condition" /opt/stock-tracker-bot/logs/bot.log

# Проблемы с подключением
grep -i "connection\|timeout" /opt/stock-tracker-bot/logs/bot.log
```

### 2. Регулярная проверка дубликатов

Запускайте скрипт проверки раз в неделю:

```bash
cd /opt/stock-tracker-bot
source venv/bin/activate
python fix_database_duplicates.py
```

### 3. Резервное копирование БД

Перед любыми изменениями делайте backup:

```bash
# Бэкап PostgreSQL
sudo -u postgres pg_dump stocktracker > backup_$(date +%Y%m%d_%H%M%S).sql
```

### 4. Настройка PostgreSQL

Проверьте настройки PostgreSQL для оптимальной работы:

```sql
-- Увеличить max_connections если нужно
SHOW max_connections;  -- Должно быть >= 100

-- Проверить idle timeout
SHOW idle_in_transaction_session_timeout;

-- Настроить statement_timeout
ALTER DATABASE stocktracker SET statement_timeout = '30s';
```

---

## 📞 Поддержка

Если после применения исправлений остаются проблемы:

1. Проверьте логи: `tail -100 /opt/stock-tracker-bot/logs/bot.log`
2. Проверьте статус: `sudo systemctl status stock-tracker-bot.service`
3. Проверьте подключение к БД: `psql -h 158.160.188.247 -U stocktracker -d stocktracker`
4. Запустите проверку дубликатов: `python fix_database_duplicates.py`

---

## 📝 История изменений

- **25.12.2025** - Найдены и исправлены критические проблемы с race condition
- **25.12.2025** - Добавлена обработка ошибок БД в middleware
- **25.12.2025** - Оптимизирован пул соединений PostgreSQL
- **25.12.2025** - Создан скрипт для исправления дубликатов
