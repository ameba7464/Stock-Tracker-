# Telegram Bot для селлеров Wildberries

Telegram-бот с воронкой регистрации для селлеров маркетплейса Wildberries. Бот собирает контактные данные пользователей и предоставляет доступ к обучающим материалам.

## 🚀 Возможности

- **Воронка регистрации**: автоматический сбор имени, email, телефона и Telegram ID
- **Интеграция с Wildberries API**: получение данных о товарах, остатках и заказах
- **Автоматическое создание Google Таблиц**: персональная таблица для каждого селлера
- **Полная аналитика**: данные из Sales Funnel, Warehouse Remains и Supplier Orders API
- **FSM (Finite State Machine)**: управление состояниями пользователя
- **Валидация данных**: проверка корректности email, телефона и API ключей
- **Два способа ввода телефона**: через кнопку Telegram или текстовый ввод
- **PostgreSQL**: надежное хранение данных пользователей и API ключей
- **Готовность к интеграции платежей**: архитектура позволяет добавить оплату без рефакторинга

## 📋 Технологический стек

- Python 3.11+
- aiogram 3.x - фреймворк для Telegram Bot API
- PostgreSQL - база данных
- SQLAlchemy 2.x - ORM для работы с БД
- asyncpg - асинхронный драйвер PostgreSQL
- pydantic - валидация конфигурации
- Google Sheets API - автоматическое создание и обновление таблиц
- Wildberries API - получение данных о товарах
- pandas - обработка данных

## 📦 Установка

### 1. Клонирование репозитория

```bash
cd telegram-bot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка базы данных PostgreSQL

Создайте базу данных:

```sql
CREATE DATABASE tgstock;
```

### 5. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Telegram Bot (получите токен у @BotFather)
BOT_TOKEN=your_bot_token_here

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tgstock
DB_USER=postgres
DB_PASSWORD=your_password

# Application
GOOGLE_SHEET_URL=https://docs.google.com/spreadsheets/d/your_sheet_id
ADMIN_IDS=123456789

# Logging
LOG_LEVEL=INFO
```

## 🎯 Получение Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (укажите имя и username бота)
4. Скопируйте полученный токен в `.env` файл

## 🏃 Запуск

```bash
python -m app.main
```

Бот начнет работать в режиме long polling.

## 📱 Использование

### Для пользователей

1. Найдите бота в Telegram по username
2. Отправьте `/start`
3. Следуйте инструкциям:
   - Введите имя
   - Введите email
   - Отправьте номер телефона (кнопкой или текстом)
4. Получите ссылку на Google-таблицу с материалами

### Команды бота

- `/start` - Начать регистрацию или вернуться в главное меню
- `/help` - Показать справку

### Главное меню

**Для пользователей без API ключа:**
- 🔑 Добавить API ключ Wildberries
- ℹ️ О боте
- ❓ Помощь

**Для пользователей с API ключом:**
- 📊 Получить мою таблицу - генерация персональной Google Таблицы с данными
- 🔑 Обновить API ключ
- ℹ️ О боте
- ❓ Помощь

## 🗂️ Структура проекта

```
telegram-bot/
├── app/
│   ├── __init__.py
│   ├── main.py                 # Точка входа
│   ├── config.py               # Конфигурация
│   │
│   ├── bot/
│   │   ├── handlers/           # Обработчики событий
│   │   │   ├── start.py        # /start, /help
│   │   │   ├── registration.py # Воронка регистрации
│   │   │   └── menu.py         # Главное меню
│   │   ├── keyboards/          # Клавиатуры
│   │   │   ├── reply.py        # ReplyKeyboard
│   │   │   └── inline.py       # InlineKeyboard
│   │   ├── middlewares/        # Middleware
│   │   │   ├── db.py           # БД сессии
│   │   │   └── payment.py      # Заглушка для платежей
│   │   └── states.py           # FSM состояния
│   │
│   ├── database/
│   │   ├── models.py           # SQLAlchemy модели
│   │   ├── database.py         # Подключение к БД
│   │   └── crud.py             # CRUD операции
│   │
│   ├── services/
│   │   └── validation.py       # Валидация данных
│   │
│   └── utils/
│       └── logger.py           # Логирование
│
├── logs/                       # Логи бота
├── .env                        # Переменные окружения (не в Git)
├── .env.example                # Пример конфигурации
├── .gitignore
├── requirements.txt            # Python зависимости
├── Dockerfile                  # Docker образ
└── README.md
```

## 🔧 Разработка

### Логирование

Логи сохраняются в папку `logs/` и выводятся в консоль. Уровень логирования настраивается в `.env`:

```env
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
```

### База данных

Модель пользователя (`User`):

- `telegram_id` - уникальный ID из Telegram
- `name` - имя пользователя
- `email` - email для связи
- `phone` - номер телефона
- `payment_status` - статус оплаты (pending/completed/failed)
- `google_sheet_sent` - флаг отправки таблицы
- `created_at` - дата регистрации
- `updated_at` - дата последнего обновления

### FSM States

Состояния воронки регистрации:

1. `GET_NAME` - ожидание ввода имени
2. `GET_EMAIL` - ожидание ввода email
3. `GET_PHONE` - ожидание ввода телефона

## 💳 Интеграция платежей (будущая функциональность)

Архитектура подготовлена для интеграции платежной системы:

### Точка интеграции

В файле `app/bot/handlers/registration.py`, функция `complete_registration()`:

```python
# ============================================
# [ТОЧКА ИНТЕГРАЦИИ ПЛАТЕЖЕЙ]
# ============================================
# В будущем здесь добавить:
# if settings.payment_enabled:
#     await send_payment_invoice(message, user)
#     await state.set_state(RegistrationStates.PAYMENT_PENDING)
#     return
# ============================================
```

### Шаги для интеграции:

1. Установить SDK платежного провайдера (YooKassa, Stripe, и т.д.)
2. Добавить FSM состояние `PAYMENT_PENDING` в `states.py`
3. Реализовать функцию `send_payment_invoice()`
4. Реализовать обработчик успешной оплаты
5. Установить `PAYMENT_ENABLED=True` в `.env`
6. Настроить параметры оплаты в `.env`

## 🐳 Docker

### Запуск с Docker Compose

```bash
docker-compose up -d
```

### Просмотр логов

```bash
docker-compose logs -f bot
```

### Остановка

```bash
docker-compose down
```

## 📝 Лицензия

MIT License

## 👥 Авторы

Разработано для селлеров Wildberries

## 📞 Поддержка

По всем вопросам обращайтесь к администратору бота.

---

**Версия:** 1.0.0  
**Дата:** 25 ноября 2025 г.
