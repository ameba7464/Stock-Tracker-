# ✅ Реализация интеграции с Wildberries API и Google Sheets

## 📋 Что сделано:

### 1. **Обновлена модель БД** ✅
- Добавлено поле `wb_api_key` (String, 500) - хранение WB API ключа
- Добавлено поле `google_sheet_id` (String, 255) - ID созданной Google Таблицы
- Создана миграция для обновления существующих БД

### 2. **Добавлены FSM состояния** ✅
- `ApiKeyStates.WAITING_FOR_API_KEY` - ожидание ввода WB API ключа

### 3. **Обновлены клавиатуры** ✅
- `get_main_menu_keyboard(has_api_key: bool)` - динамическое меню:
  - **Без API ключа**: "🔑 Добавить API ключ Wildberries"
  - **С API ключом**: "📊 Получить мою таблицу" + "🔑 Обновить API ключ"

### 4. **Созданы обработчики** ✅

**`app/bot/handlers/api_key.py`:**
- `callback_add_api_key` - показ инструкции по получению API ключа
- `process_api_key` - валидация и сохранение API ключа
- `callback_generate_table` - генерация персональной таблицы

### 5. **Созданы сервисы** ✅

**`app/services/wb_integration.py`:**
- `validate_api_key()` - валидация WB API ключа через реальный запрос
- `generate_or_get_table()` - генерация или обновление таблицы
- `_fetch_wb_data()` - получение данных из WB API (использует `wildberries_complete_data_collector.py`)

**`app/services/google_sheets.py`:**
- `create_sheet()` - создание новой Google Таблицы
- `update_sheet()` - обновление существующей таблицы
- `_prepare_table_data()` - преобразование данных WB в формат таблицы
- `_format_sheet()` - форматирование (заголовки, цвета, заморозка строк)

### 6. **Интеграция с существующим кодом** ✅
- Используется `WildberriesDataCollector` из `wildberries_complete_data_collector.py`
- Метод `collect_complete_data()` для получения полных данных из 3 API:
  - Sales Funnel API v3
  - Warehouse Remains API
  - Supplier Orders API
- Все метрики из существующей структуры `ProductMetrics`

### 7. **CRUD операции** ✅
- `update_user_api_key()` - сохранение WB API ключа
- Обновлена модель User для хранения `google_sheet_id`

### 8. **Документация** ✅
- `GOOGLE_SERVICE_ACCOUNT_SETUP.md` - пошаговая инструкция настройки
- Обновлен `README.md` с новой функциональностью
- Обновлен `.gitignore` (добавлен `credentials.json`)

### 9. **Зависимости** ✅
Добавлены в `requirements.txt`:
- `google-auth` - авторизация Google API
- `google-api-python-client` - работа с Google Sheets
- `pandas` - обработка данных
- `requests` - HTTP запросы (уже используется в WB collector)

---

## 🎯 Как это работает:

### **Сценарий 1: Добавление API ключа**

```
1. Пользователь завершил регистрацию
2. В главном меню видит "🔑 Добавить API ключ Wildberries"
3. Нажимает кнопку
4. Бот показывает инструкцию по получению ключа из WB личного кабинета
5. Пользователь вводит API ключ
6. Бот валидирует ключ через реальный запрос к WB API
7. При успехе - сохраняет в БД и обновляет меню
```

### **Сценарий 2: Генерация таблицы**

```
1. Пользователь с API ключом нажимает "📊 Получить мою таблицу"
2. Бот показывает "⏳ Генерация таблицы началась..."
3. Бот запрашивает данные из WB API (используя wildberries_complete_data_collector):
   - Sales Funnel (аналитика, конверсии)
   - Warehouse Remains (остатки по складам)
   - Supplier Orders (заказы, в пути)
4. Если таблицы нет:
   - Создает новую Google Таблицу: "Stock Tracker - {Имя пользователя}"
   - Автоматически дает доступ "Редактор" всем у кого есть ссылка
   - Сохраняет ID таблицы в БД
5. Если таблица есть:
   - Обновляет существующую таблицу новыми данными
6. Заполняет таблицу структурой из wildberries_complete_data_collector:
   - Основная информация (бренд, предмет, артикулы)
   - Общие метрики (заказы, остатки, оборачиваемость)
   - Разбивка по складам (остатки, заказы, оборачиваемость по каждому)
7. Форматирует таблицу (цвета, заморозка заголовков, автоширина)
8. Отправляет пользователю ссылку на таблицу
```

### **Сценарий 3: Повторный запрос таблицы**

```
1. Пользователь снова нажимает "📊 Получить мою таблицу"
2. Бот берет сохраненный google_sheet_id из БД
3. Обновляет существующую таблицу актуальными данными из WB
4. Отправляет ту же ссылку (данные обновлены)
```

---

## 🔧 Что нужно сделать для запуска:

### 1. Установить зависимости
```bash
cd telegram-bot
pip install -r requirements.txt
```

### 2. Настроить Google Service Account
См. файл `GOOGLE_SERVICE_ACCOUNT_SETUP.md`

**Кратко:**
1. Создать проект в Google Cloud Console
2. Включить Google Sheets API и Google Drive API
3. Создать Service Account
4. Скачать `credentials.json`
5. Поместить файл в `telegram-bot/credentials.json`

### 3. Применить миграцию БД
```bash
cd telegram-bot
alembic upgrade head
```

Или пересоздать БД:
```bash
rm tgstock  # удалить старую БД
python -m app.main  # при запуске создастся новая с нужными полями
```

### 4. Запустить бота
```bash
python -m app.main
```

---

## 📊 Структура данных в таблице:

### **Основная информация** (4 колонки)
- Бренд
- Предмет
- Артикул продавца
- Артикул товара (nmid)

### **Общие метрики** (6 колонок)
- В пути до покупателя
- В пути конв. на склад WB
- Всего заказов на складах WB
- Заказы (всего)
- Остатки (всего)
- Оборачиваемость (дни)

### **Разбивка по складам** (динамически, по 3 колонки на склад)
Для каждого физического склада WB:
- Остатки
- Заказы
- Оборачиваемость

**Пример:**
```
Коледино          | Подольск          | Электросталь
Остатки|Заказы|Об.| Остатки|Заказы|Об.| Остатки|Заказы|Об.
100    |50    |2.0| 75     |30    |2.5| 150    |20    |7.5
```

---

## ✅ Преимущества реализации:

1. **Использует существующий код** - никакой дублирования логики WB API
2. **Полная интеграция** - все 3 API источника данных
3. **Персональные таблицы** - каждый селлер получает свою таблицу
4. **Автообновление** - при повторном запросе данные обновляются
5. **Автодоступ** - пользователь сразу может редактировать таблицу
6. **Масштабируемость** - легко добавить новые метрики или склады
7. **Безопасность** - API ключи хранятся в БД, не передаются пользователю

---

## 🔄 Архитектура взаимодействия:

```
Telegram Bot (aiogram)
    ↓
Handlers (api_key.py)
    ↓
Services (wb_integration.py) ────→ wildberries_complete_data_collector.py
    ↓                                          ↓
Services (google_sheets.py)          WildberriesDataCollector
    ↓                                          ↓
Google Sheets API                    Wildberries API (3 endpoints)
    ↓                                          ↓
Персональная таблица               ProductMetrics (полные данные)
```

---

**Дата:** 25 ноября 2025 г.
**Статус:** ✅ ГОТОВО К ТЕСТИРОВАНИЮ
