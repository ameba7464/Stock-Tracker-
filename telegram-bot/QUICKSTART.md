# 🚀 Быстрый старт

## ✅ Что реализовано

### 1. Автообновление таблиц (00:01 МСК)
- ✅ Планировщик APScheduler
- ✅ Автоматическое обновление всех пользовательских таблиц раз в сутки
- ✅ Логирование процесса обновления
- ✅ Обработка ошибок

### 2. Название листа "Stock Tracker"
- ✅ Автоматическое переименование при создании таблицы
- ✅ Переименование при обновлении (если нужно)

### 3. Исправления
- ✅ SSL ошибки WB API (отключена верификация)
- ✅ Полное форматирование таблиц (объединение ячеек, границы, цвета)

---

## 🏃 Локальный запуск

### 1. Установите зависимости

```powershell
cd "c:\Users\miros\Downloads\Stock Tracker\Stock-Tracker\telegram-bot"
.\.venv\Scripts\pip.exe install -r requirements.txt
```

### 2. Запустите бота

```powershell
.\.venv\Scripts\python.exe -m app.main
```

### 3. Проверьте логи

Вы должны увидеть:

```
✅ Scheduler started successfully
⏰ Next update scheduled for: 00:01 MSK daily
📅 Next run time: 2025-11-27 00:01:00+03:00
```

---

## ☁️ Деплой в Yandex Cloud

Полная инструкция: **[YANDEX_CLOUD_DEPLOY.md](./YANDEX_CLOUD_DEPLOY.md)**

### Кратко:

```bash
# 1. Соберите Docker образ
docker build -t cr.yandex/<REGISTRY_ID>/stock-tracker-bot:latest .

# 2. Загрузите в реестр
docker push cr.yandex/<REGISTRY_ID>/stock-tracker-bot:latest

# 3. Создайте ВМ с контейнером
yc compute instance create-with-container \
  --name stock-tracker-bot-vm \
  --container-image cr.yandex/<REGISTRY_ID>/stock-tracker-bot:latest \
  --container-env-file .env \
  --container-restart-policy always
```

---

## 📊 Проверка автообновления

### Способ 1: Логи бота

```
2025-11-27 00:01:00 - tgstock - INFO - 🔄 AUTOMATIC TABLE UPDATE STARTED
2025-11-27 00:01:00 - tgstock - INFO - ⏰ Time: 2025-11-27 00:01:00
2025-11-27 00:01:00 - tgstock - INFO - 📊 Found 5 users with configured tables
2025-11-27 00:01:15 - tgstock - INFO - ✅ [1/5] User 1651759646 updated successfully
...
2025-11-27 00:02:30 - tgstock - INFO - ✅ Successfully updated: 5/5
```

### Способ 2: Ручной запуск

Для тестирования можно запустить обновление вручную:

```python
from app.services.scheduler import auto_update_scheduler
import asyncio

asyncio.run(auto_update_scheduler.run_manual_update())
```

---

## 🗂️ Структура файлов

```
telegram-bot/
├── app/
│   ├── main.py                 # Точка входа, запуск scheduler
│   ├── services/
│   │   ├── scheduler.py        # Планировщик автообновления (НОВЫЙ)
│   │   ├── google_sheets.py    # Форматирование таблиц (ОБНОВЛЁН)
│   │   └── wb_integration.py   # WB API интеграция
│   └── ...
├── credentials.json            # Google Service Account
├── token.json                  # OAuth токен
├── .env                        # Переменные окружения
├── Dockerfile                  # Docker образ (ОБНОВЛЁН)
├── requirements.txt            # Зависимости (добавлен apscheduler)
├── YANDEX_CLOUD_DEPLOY.md     # Инструкция по деплою (НОВАЯ)
└── QUICKSTART.md              # Этот файл (НОВЫЙ)
```

---

## ⚙️ Переменные окружения

Файл `.env`:

```env
# Telegram Bot
BOT_TOKEN=8558236991:AAHFu2krkBMIWFKF6W_MkIYoIFbfw-d1kms

# Database
DATABASE_URL=sqlite+aiosqlite:///./database.db

# Google Drive
GOOGLE_DRIVE_FOLDER_ID=1NkBvCFyFpXRg8Opno6-_Cf8mTeT7OHRA

# Logging
LOG_LEVEL=INFO

# Timezone (для корректной работы scheduler)
TZ=Europe/Moscow
```

---

## 🎯 Основные функции

### AutoUpdateScheduler

Класс в `app/services/scheduler.py`:

- **`start()`** - запуск планировщика
- **`stop()`** - остановка планировщика
- **`update_all_user_tables()`** - обновление всех таблиц (вызывается автоматически в 00:01)
- **`run_manual_update()`** - ручной запуск обновления
- **`get_next_run_time()`** - время следующего запланированного обновления

### Google Sheets форматирование

Обновлённый `_format_sheet()` в `app/services/google_sheets.py`:

- ✅ Объединение ячеек заголовков ("Основная информация", "Общие метрики", склады)
- ✅ Синий фон заголовков (#6B7DEB)
- ✅ Толстые границы между секциями
- ✅ Правильные размеры колонок и строк
- ✅ Автоматическая заморозка 2 строк заголовков

---

## 🐛 Troubleshooting

### Scheduler не запускается

Проверьте логи:

```
ERROR - Failed to start scheduler: ...
```

Убедитесь, что apscheduler установлен:

```powershell
.\.venv\Scripts\pip.exe list | Select-String apscheduler
```

### Таблицы не обновляются

1. Проверьте, что у пользователей есть:
   - `wb_api_key` (WB API ключ)
   - `google_sheet_id` (ID таблицы)

2. Проверьте логи на наличие ошибок WB API или Google Sheets API

### SSL ошибки WB API

Уже исправлено! В `wildberries_complete_data_collector.py`:

```python
# Отключаем SSL верификацию
self.session.verify = False
```

---

## 📞 Поддержка

При возникновении проблем проверьте:

1. **Логи бота**: `logs/bot.log`
2. **Логи Docker**: `docker logs <CONTAINER_ID>`
3. **Статус scheduler**: Должна быть запись "✅ Scheduler started successfully"

---

## 🎉 Готово!

Бот полностью готов к работе в 24/7 режиме с автоматическим обновлением таблиц! 🚀

**Следующие шаги:**
1. Запустите бота локально для тестирования
2. Проверьте создание и форматирование таблиц
3. Дождитесь 00:01 для проверки автообновления
4. Задеплойте в Yandex Cloud для постоянной работы
