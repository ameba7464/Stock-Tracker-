# 🚀 Быстрый запуск админ-панели

## 1. Примените миграцию

```powershell
cd Stock-Tracker
.venv\Scripts\Activate.ps1
alembic upgrade head
```

## 2. Создайте первого администратора

```powershell
python scripts/create_admin.py grant ваш_email@example.com
```

Или вручную в PostgreSQL:
```sql
UPDATE users SET is_admin = true WHERE email = 'ваш_email@example.com';
```

## 3. Запустите приложение

```powershell
python main.py
```

Или:
```powershell
uvicorn stock_tracker.api.main:app --reload
```

## 4. Войдите в админ-панель

1. Откройте в браузере: http://localhost:8000/admin/login.html
2. Введите свои учетные данные
3. После входа вы будете перенаправлены на админ-панель

## 📋 Доступные команды для управления администраторами

### Выдать права администратора
```powershell
python scripts/create_admin.py grant email@example.com
```

### Забрать права администратора
```powershell
python scripts/create_admin.py revoke email@example.com
```

### Показать всех администраторов
```powershell
python scripts/create_admin.py list
```

## 🎯 Что дальше?

- Прочитайте полную документацию: `docs/ADMIN_PANEL_GUIDE.md`
- Изучите API эндпоинты: http://localhost:8000/docs
- Настройте дизайн под себя в `src/stock_tracker/static/admin.html`

## ⚠️ Важно!

- Никогда не выдавайте права администратора пользователям, которым не доверяете
- Администраторы могут управлять всеми пользователями и их данными
- Храните учетные данные администратора в безопасности
- Используйте сильные пароли для админских аккаунтов
