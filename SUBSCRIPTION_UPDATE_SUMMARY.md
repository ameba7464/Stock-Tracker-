# Обновление Архитектуры: Унификация Системы Подписок

**Дата:** 25 декабря 2025 г.  
**Тип изменения:** Критическое архитектурное улучшение  

---

## 🎯 Ключевое Решение

### Проблема (до обновления)

В проекте существовало **две несовместимые системы подписок**:

1. **Telegram Bot** — использовал поле `payment_status` в таблице `users` с enum'ом "free"/"completed"
2. **Backend/Admin** — использовал отдельную таблицу `subscriptions` с полями `has_access`/`status`

**Конфликты:**
- Невозможно синхронизировать статусы между системами
- Разные enum'ы (lowercase vs UPPERCASE)
- Два источника правды о доступе пользователя
- Временный статус "completed" только для MVP
- Невозможность легко включить платежи

---

## ✅ Решение: Унифицированная Архитектура

### Что сделано

**1. Создан архитектурный документ**
- Файл: [SUBSCRIPTION_ARCHITECTURE.md](Stock-Tracker/SUBSCRIPTION_ARCHITECTURE.md)
- Полное описание проблемы и решения
- План миграции в 3 фазы
- FAQ и примеры использования

**2. Создана миграция БД**
- Файл: [20251225_unify_subscriptions.py](Stock-Tracker/migrations/versions/20251225_unify_subscriptions.py)
- Создает записи `subscriptions` для всех telegram пользователей
- Добавляет новые колонки для метаданных платежей
- Конвертирует `status` в enum тип
- Добавляет индексы и constraints

**3. Создан сервис подписок для бота**
- Файл: [subscription.py](Stock-Tracker/telegram-bot/app/services/subscription.py)
- `check_user_access()` — единая проверка доступа с feature flag
- `get_or_create_subscription()` — безопасное создание подписок
- `grant_paid_access()` — выдача доступа после оплаты
- `revoke_access()` — отзыв доступа
- `get_subscription_info()` — информация о подписке

**4. Обновлена модель данных бота**
- Файл: [models.py](Stock-Tracker/telegram-bot/app/database/models.py)
- Добавлен класс `Subscription` с полным набором полей
- Добавлен `SubscriptionStatus` enum
- Методы `grant_access()` и `revoke_access()`

**5. Обновлена конфигурация**
- Файл: [config.py](Stock-Tracker/telegram-bot/app/config.py)
- Добавлен `free_trial_days: int = 7`
- Добавлен `subscription_price: int = 299`
- Улучшены комментарии к `payment_enabled`

**6. Обновлена регистрация пользователей**
- Файл: [registration.py](Stock-Tracker/telegram-bot/app/bot/handlers/registration.py)
- Используется `get_or_create_subscription()`
- Feature flag проверка для включения платежей
- Подготовлено место для интеграции payment provider

**7. Обновлено главное меню**
- Файл: [menu.py](Stock-Tracker/telegram-bot/app/bot/handlers/menu.py)
- Заменена проверка `payment_status` на `check_user_access()`
- Единая логика доступа для всех хендлеров

---

## 🚀 Как Работает

### MVP Режим (Сейчас)

```python
# В .env файле
PAYMENT_ENABLED=false

# Поведение системы:
# 1. Новый пользователь регистрируется
# 2. Создается subscription со статусом FREE и has_access=True
# 3. check_user_access() возвращает True для всех (payment_enabled=False)
# 4. Пользователь получает доступ сразу без оплаты
```

### Продакшн Режим (Будущее)

```python
# В .env файле
PAYMENT_ENABLED=true
PAYMENT_PROVIDER=yookassa
PAYMENT_TOKEN=your_token_here
FREE_TRIAL_DAYS=7

# Поведение системы:
# 1. Новый пользователь регистрируется
# 2. Создается subscription со статусом TRIAL и has_access=True
# 3. trial_ends_at = NOW() + 7 days
# 4. После 7 дней: check_user_access() проверяет истек ли триал
# 5. Если истек и не оплачено → has_access=False, status=EXPIRED
# 6. После оплаты → grant_paid_access() устанавливает PAID статус
```

### Legacy Users (Защита существующих)

```python
# Существующие пользователи при включении платежей:
# - Получают status=FREE с has_access=True
# - Бесплатный доступ навсегда (grandfathered)
# - Не требуется оплата
```

---

## 📋 План Внедрения

### Фаза 1: Подготовка (✅ Завершена)

- [x] Создать документацию SUBSCRIPTION_ARCHITECTURE.md
- [x] Создать миграцию 20251225_unify_subscriptions.py
- [x] Создать subscription service
- [x] Обновить модели бота
- [x] Обновить конфиг
- [x] Обновить registration.py
- [x] Обновить menu.py

### Фаза 2: Миграция БД (Следующий шаг)

```bash
# 1. Проверить текущую миграцию
cd Stock-Tracker
alembic current

# 2. Обновить down_revision в 20251225_unify_subscriptions.py
# Вставить ID текущей миграции

# 3. Применить миграцию
alembic upgrade head

# 4. Проверить результаты
# - Таблица subscriptions заполнена
# - Все telegram пользователи имеют status=FREE
# - Новые колонки добавлены
```

### Фаза 3: Тестирование

```bash
# 1. Запустить бота
cd telegram-bot
python -m app.main

# 2. Протестировать регистрацию нового пользователя
# - Должна создаться запись в subscriptions
# - status=FREE, has_access=True

# 3. Протестировать доступ существующего пользователя
# - check_user_access() должна вернуть True
# - Меню должно работать корректно

# 4. Убедиться что payment_enabled=False
# - Все имеют доступ
# - Платежи не запрашиваются
```

### Фаза 4: Переход на Платежи (Когда захотите)

```env
# Просто измените в .env:
PAYMENT_ENABLED=true
PAYMENT_PROVIDER=yookassa
PAYMENT_TOKEN=live_token_here

# Перезапустите бота - все готово!
```

---

## 🎯 Преимущества Решения

### ✅ Для MVP (Сейчас)

1. **Не ломает работу** — бот продолжает работать бесплатно
2. **Защита данных** — все существующие пользователи сохраняют доступ
3. **Простота** — минимальные изменения в текущем коде

### ✅ Для Будущего (Платежи)

1. **Один переключатель** — `PAYMENT_ENABLED=true` включает оплату
2. **Нет изменений кода** — логика уже готова
3. **Гибкость** — легко добавить разные тарифы
4. **Масштабируемость** — интеграция любых payment providers

### ✅ Для Архитектуры

1. **Единый источник правды** — таблица subscriptions
2. **Нет конфликтов** — backend и bot используют одну логику
3. **Тестируемость** — легко переключаться между режимами
4. **Расширяемость** — можно добавить analytics, referrals, etc

---

## 📊 Обновление Оценки Архитектуры

### Было: 7/10

**Слабые места:**
- ~~Две несовместимые системы подписок~~
- ~~Невозможность легко включить платежи~~
- ~~Конфликты между bot и backend~~

### Стало: 8.5/10

**Улучшения:**
- ✅ Унифицированная система подписок
- ✅ Feature flag для монетизации
- ✅ Единый источник правды
- ✅ Обратная совместимость
- ✅ Защита legacy users

**Осталось для 10/10:**
- Шифрование API ключей (уже подготовлено)
- Audit logging (уже подготовлено)
- Партиционирование sync_logs
- CQRS/Event Sourcing для аналитики
- CDC для репликации

---

## 📖 См. также

- [SUBSCRIPTION_ARCHITECTURE.md](Stock-Tracker/SUBSCRIPTION_ARCHITECTURE.md) — Полная документация
- [20251225_unify_subscriptions.py](Stock-Tracker/migrations/versions/20251225_unify_subscriptions.py) — Миграция БД
- [subscription.py](Stock-Tracker/telegram-bot/app/services/subscription.py) — Сервис подписок
- [DATABASE_ARCHITECTURE_AUDIT.md](DATABASE_ARCHITECTURE_AUDIT.md) — Общий аудит архитектуры

---

**Создано:** 25 декабря 2025 г.  
**Статус:** ✅ Готово к внедрению
