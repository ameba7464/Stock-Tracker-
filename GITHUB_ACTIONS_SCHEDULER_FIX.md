# 🔧 Исправление проблемы с GitHub Actions Scheduler

## 🔍 Диагностика проблемы

### Найденные проблемы:
1. ✅ Workflow файл находится в ветке `main` (правильно)
2. ✅ Синтаксис cron правильный: `'1 21 * * *'` (запуск в 21:01 UTC = 00:01 МСК)
3. ⚠️ **Возможная проблема**: GitHub может отключать scheduled workflows в неактивных репозиториях

## 🛠️ Применённые исправления

### 1. Добавлен триггер на push
```yaml
push:
  branches:
    - main
  paths:
    - '.github/workflows/update-stocks.yml'
```
Это активирует workflow при изменении файла, что "разбудит" scheduler.

### 2. Добавлены permissions
```yaml
permissions:
  contents: write
```
Это гарантирует, что workflow имеет необходимые права.

## 🎯 Решение проблемы: Пошаговая инструкция

### Шаг 1: Закоммитьте изменения
```bash
cd "c:\Users\miros\Downloads\Stock Tracker\Stock-Tracker"
git add .github/workflows/update-stocks.yml
git commit -m "fix: improve GitHub Actions scheduler reliability"
git push origin main
```

### Шаг 2: Проверьте настройки в GitHub

1. **Перейдите на GitHub**: https://github.com/ameba7464/Stock-Tracker-
2. **Actions tab**: Проверьте, что workflow появился
3. **Settings → Actions → General**: 
   - Убедитесь, что Actions включены
   - Workflow permissions должны быть "Read and write permissions"

### Шаг 3: Активируйте workflow вручную

После push изменений:
1. Зайдите в **Actions** → **Update Stock Tracker Daily**
2. Нажмите **Run workflow** → **Run workflow** (зелёная кнопка)
3. Это "разбудит" scheduler

### Шаг 4: Проверка scheduled runs

После ручного запуска GitHub должен начать выполнять scheduled runs. Проверьте через 24 часа.

## 🚨 Известные проблемы GitHub Actions Scheduler

### 1. Отключение в неактивных репозиториях
**Симптом**: Workflow не запускается по расписанию
**Причина**: GitHub отключает scheduled workflows после 60 дней неактивности
**Решение**: 
- Сделайте любой commit в репозиторий
- Или запустите workflow вручную раз в 2 месяца

### 2. Задержки при высокой нагрузке
**Симптом**: Workflow запускается с задержкой 5-15 минут
**Причина**: GitHub может задерживать scheduled jobs при высокой нагрузке
**Решение**: Это нормально, ничего делать не нужно

### 3. Не запускается в форках
**Симптом**: Scheduled events не работают в форках
**Причина**: Ограничение безопасности GitHub
**Решение**: Используйте оригинальный репозиторий

## 📊 Мониторинг

### Проверка статуса workflow:
```bash
# Через GitHub CLI (если установлен)
gh workflow list
gh run list --workflow=update-stocks.yml --limit 5
```

### Проверка логов:
1. GitHub → Actions → Update Stock Tracker Daily
2. Выберите конкретный run
3. Просмотрите логи каждого step

## ✅ Checklist для диагностики

- [ ] Workflow файл находится в default branch (main)
- [ ] Actions включены в Settings → Actions
- [ ] Secrets настроены правильно:
  - `WILDBERRIES_API_KEY`
  - `GOOGLE_SERVICE_ACCOUNT`
  - `GOOGLE_SHEET_ID`
  - `GOOGLE_SHEET_NAME`
- [ ] Нет сообщений об ошибках в последнем ручном запуске
- [ ] Репозиторий активен (были коммиты за последние 60 дней)
- [ ] Workflow был запущен вручную хотя бы один раз после создания

## 🔄 Альтернативные решения

### Решение 1: Использовать external cron service
Если GitHub scheduler продолжает не работать, можно использовать:
- **GitHub API + внешний cron** (cron-job.org, EasyCron)
- **Webhook trigger** с внешнего сервиса

### Решение 2: Self-hosted runner
Запустить свой runner с настоящим cron:
```bash
# На вашем сервере
0 0 * * * curl -X POST -H "Authorization: token YOUR_GITHUB_TOKEN" \
  https://api.github.com/repos/ameba7464/Stock-Tracker-/actions/workflows/update-stocks.yml/dispatches \
  -d '{"ref":"main"}'
```

## 📝 Дополнительная информация

### Формат cron в GitHub Actions:
```
┌───────────── минута (0 - 59)
│ ┌───────────── час (0 - 23)
│ │ ┌───────────── день месяца (1 - 31)
│ │ │ ┌───────────── месяц (1 - 12)
│ │ │ │ ┌───────────── день недели (0 - 6) (Sunday to Saturday)
│ │ │ │ │
* * * * *
```

Текущее: `1 21 * * *` = каждый день в 21:01 UTC (00:01 МСК)

### Полезные ссылки:
- [GitHub Actions: Events that trigger workflows](https://docs.github.com/en/actions/using-workflows/events-that-trigger-workflows#schedule)
- [Scheduled workflows FAQ](https://docs.github.com/en/actions/using-workflows/events-that-trigger-workflows#scheduled-events)

---
**Дата создания**: 15 ноября 2025  
**Статус**: ✅ Исправления применены, ожидается тестирование
