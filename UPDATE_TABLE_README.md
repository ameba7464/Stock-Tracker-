# Руководство по использованию update_table_fixed.py

## 🚀 Описание

Скрипт для обновления таблицы Google Sheets свежими данными из Wildberries API с использованием Dual API (FBO + FBS).

## ✨ Возможности

- ✅ **Dual API**: Statistics API (FBO) + Marketplace API v3 (FBS)
- ✅ **Автоматическое повторение** при временных ошибках API (503, quota)
- ✅ **Фильтрация отменённых заказов** (isCancel=True)
- ✅ **Дедупликация заказов** по srid
- ✅ **Нормализация названий складов**
- ✅ **Оптимизация API запросов** (экономия ~58%)

## 📋 Требования

1. Python 3.11+
2. Виртуальное окружение с установленными зависимостями
3. Файл `.env` с настройками
4. Файл `config/service-account.json` с ключами Google API

## 🔧 Установка зависимостей

```bash
# Из директории Stock-Tracker
pip install -r requirements.txt
```

## 🎯 Использование

### Базовый запуск (использует настройки из .env)

```bash
python update_table_fixed.py
```

### С указанием ID таблицы и названия листа

```bash
python update_table_fixed.py [spreadsheet_id] [worksheet_name]
```

### Примеры

```bash
# Использовать настройки из .env
python update_table_fixed.py

# Указать конкретную таблицу
python update_table_fixed.py 1abc123def456ghi789jkl "Stock Tracker"

# Показать справку
python update_table_fixed.py --help
```

## 🔄 Механизм повторных попыток

Скрипт автоматически повторяет операции при временных ошибках:

- **503 Service Unavailable**: 3 попытки с интервалами 5, 10, 20 секунд
- **Quota exceeded**: 2 попытки с интервалами 10, 20 секунд
- **Экспоненциальная задержка**: каждая следующая попытка удваивает время ожидания

## ⚠️ Типичные ошибки и решения

### 1. Configuration validation failed

**Причина**: Отсутствуют переменные окружения в `.env`

**Решение**:
```bash
# Проверьте наличие файла .env в директории Stock-Tracker
# Убедитесь, что в нём есть:
WILDBERRIES_API_KEY=ваш_ключ
GOOGLE_SHEET_ID=ваш_id_таблицы
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=./config/service-account.json
```

### 2. APIError: [503]: Service unavailable

**Причина**: Google Sheets API временно недоступен

**Решение**: Скрипт автоматически повторит попытку. Если не помогло - подождите несколько минут.

### 3. ModuleNotFoundError

**Причина**: Не установлены зависимости

**Решение**:
```bash
pip install -r requirements.txt
```

### 4. Permission denied / 403

**Причина**: Service account не имеет доступа к таблице

**Решение**: Предоставьте email сервисного аккаунта доступ к Google Sheets

## 📊 Что делает скрипт

1. **Загружает конфигурацию** из `.env`
2. **Подключается к Google Sheets** с использованием service account
3. **Очищает старые данные** из указанного листа (с повторными попытками)
4. **Получает данные из Wildberries**:
   - Statistics API → остатки FBO (склады WB)
   - Marketplace API v3 → остатки FBS (склад продавца)
   - Supplier Orders API → заказы за последние 7 дней
5. **Обрабатывает данные**:
   - Фильтрует отменённые заказы
   - Дедуплицирует по srid
   - Нормализует названия складов
   - Рассчитывает оборачиваемость (целые числа)
6. **Записывает в Google Sheets** с оптимизацией запросов

## 🧪 Тестирование

```bash
# Проверка всех компонентов
python test_update_script.py

# Проверка загрузки конфигурации
python test_config_loading.py

# Проверка инициализации
python test_update_initialization.py

# Проверка механизма повторных попыток
python test_retry_mechanism.py
```

## 📝 Логирование

Логи сохраняются в директории `logs/` с уровнем детализации согласно `LOG_LEVEL` в `.env`

## 🔐 Безопасность

- **Не коммитьте** `.env` и `config/service-account.json` в Git
- Используйте `.gitignore` для исключения конфиденциальных файлов
- Регулярно обновляйте API ключи

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в директории `logs/`
2. Убедитесь, что все зависимости установлены
3. Проверьте доступность Google Sheets API
4. Проверьте валидность API ключей Wildberries

## 🚦 Статус выполнения

Скрипт выводит подробную информацию о каждом этапе:
- 🚀 Запуск
- 📋 Загрузка конфигурации
- 🔐 Подключение к Google Sheets
- 🧹 Очистка данных
- 📥 Получение данных из Wildberries
- 💾 Запись в Google Sheets
- ✅ Успешное завершение
- ❌ Ошибки (с советами по исправлению)

## 🎉 История изменений

### v2.0 (10.11.2025)
- ✅ Добавлен механизм повторных попыток при ошибках API
- ✅ Исправлена загрузка конфигурации при запуске из другой директории
- ✅ Улучшена обработка ошибок с подсказками
- ✅ Оборачиваемость по складам теперь целое число

### v1.0 (28.10.2025)
- ✅ Переход на Dual API (FBO + FBS)
- ✅ Добавлена поддержка Marketplace API v3
- ✅ Оптимизация API запросов (экономия 58%)
