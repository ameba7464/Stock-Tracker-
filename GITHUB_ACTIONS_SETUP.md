# 🚀 Настройка автоматического обновления через GitHub Actions

Это руководство поможет настроить автоматическое обновление таблицы Stock Tracker каждый день в **00:01 по МСК** через GitHub Actions.

## 📋 Содержание

1. [Что это даёт](#что-это-даёт)
2. [Требования](#требования)
3. [Пошаговая настройка](#пошаговая-настройка)
4. [Проверка работы](#проверка-работы)
5. [Устранение проблем](#устранение-проблем)

---

## 🎯 Что это даёт

✅ **Автоматическое обновление 24/7** — таблица обновляется даже когда компьютер выключен
✅ **Надёжность** — работает на серверах GitHub
✅ **Бесплатно** — для публичных репозиториев неограниченно, для приватных 2000 минут/месяц
✅ **Логи** — полная история всех запусков с детальными логами
✅ **Контроль** — можно запускать вручную когда нужно
✅ **Безопасность** — чувствительные данные хранятся в зашифрованных Secrets

---

## 📦 Требования

Перед началом убедитесь, что у вас есть:

- ✅ GitHub аккаунт
- ✅ Репозиторий Stock-Tracker (публичный или приватный)
- ✅ API токен Wildberries
- ✅ Service Account для Google Sheets API
- ✅ ID Google Sheets документа

---

## 🔧 Пошаговая настройка

### Шаг 1: Получение Google Service Account

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите **Google Sheets API**:
   - Перейдите в "APIs & Services" → "Library"
   - Найдите "Google Sheets API"
   - Нажмите "Enable"

4. Создайте Service Account:
   - Перейдите в "IAM & Admin" → "Service Accounts"
   - Нажмите "Create Service Account"
   - Укажите имя: `stock-tracker-bot`
   - Нажмите "Create and Continue"
   - Пропустите разрешения (Grant this service account access)
   - Нажмите "Done"

5. Создайте ключ:
   - Кликните на созданный Service Account
   - Перейдите на вкладку "Keys"
   - Нажмите "Add Key" → "Create new key"
   - Выберите формат **JSON**
   - Сохраните файл (он автоматически скачается)

6. **Важно!** Откройте скачанный JSON файл и скопируйте **весь его содержимое**

7. Дайте доступ к Google Sheets:
   - Откройте ваш Google Sheets документ
   - Нажмите "Поделиться" (Share)
   - Вставьте email из Service Account (вида `stock-tracker-bot@project-id.iam.gserviceaccount.com`)
   - Дайте права "Редактор" (Editor)

### Шаг 2: Настройка GitHub Secrets

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret** и добавьте следующие секреты:

#### 1️⃣ `GOOGLE_SERVICE_ACCOUNT`
- **Значение**: Вставьте весь JSON контент из файла service account
- **Формат**:
```json
{
  "type": "service_account",
  "project_id": "your-project-id",
  "private_key_id": "xxx",
  "private_key": "-----BEGIN PRIVATE KEY-----\nXXX\n-----END PRIVATE KEY-----\n",
  "client_email": "stock-tracker-bot@your-project.iam.gserviceaccount.com",
  "client_id": "123456789",
  "auth_uri": "https://accounts.google.com/o/oauth2/auth",
  "token_uri": "https://oauth2.googleapis.com/token",
  "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
  "client_x509_cert_url": "https://www.googleapis.com/robot/v1/metadata/x509/..."
}
```

#### 2️⃣ `WILDBERRIES_API_KEY`
- **Значение**: Ваш API токен от Wildberries
- **Пример**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`
- Получить можно в личном кабинете WB: Профиль → Настройки → Доступ к API

#### 3️⃣ `GOOGLE_SHEET_ID`
- **Значение**: ID вашего Google Sheets документа
- **Как найти**: Откройте таблицу, ID находится в URL
```
https://docs.google.com/spreadsheets/d/[ЭТО_ID]/edit
```
- **Пример**: `1aBc123DeFg456HiJk789LmNoPqRsTuVwXyZ`

#### 4️⃣ `GOOGLE_SHEET_NAME` (опционально)
- **Значение**: Название листа в таблице
- **По умолчанию**: `Stock Tracker`
- Если у вас лист называется по-другому, укажите его название

### Шаг 3: Отправка файлов в GitHub

Если вы ещё не загрузили `.github/workflows/update-stocks.yml` в репозиторий:

```powershell
# Перейдите в папку проекта
cd "C:\Users\miros\Downloads\Stock Tracker\Stock-Tracker"

# Добавьте файлы
git add .github/workflows/update-stocks.yml
git add update_table_fixed.py
git add GITHUB_ACTIONS_SETUP.md

# Создайте коммит
git commit -m "feat: Add GitHub Actions for automatic daily updates at 00:01 MSK"

# Отправьте на GitHub
git push origin main
```

---

## ✅ Проверка работы

### Проверка 1: Запуск вручную

1. Перейдите в ваш репозиторий на GitHub
2. Откройте вкладку **Actions**
3. Выберите workflow **"Update Stock Tracker Daily"**
4. Нажмите **"Run workflow"** → **"Run workflow"**
5. Дождитесь завершения (обычно 1-3 минуты)
6. Проверьте логи — они должны показывать успешное обновление

### Проверка 2: Автоматический запуск

- Workflow будет запускаться автоматически каждый день в **00:01 МСК** (21:01 UTC)
- Проверить можно на следующий день во вкладке **Actions**

### Проверка 3: Google Sheets

1. Откройте вашу таблицу Google Sheets
2. Проверьте, что данные обновились
3. Можно проверить timestamp последнего обновления

---

## 🐛 Устранение проблем

### ❌ Ошибка: "Error loading credentials"

**Проблема**: Неправильно настроен `GOOGLE_SERVICE_ACCOUNT`

**Решение**:
1. Убедитесь, что вы скопировали **весь** JSON контент
2. JSON должен начинаться с `{` и заканчиваться `}`
3. Проверьте, что не добавлены лишние пробелы или переносы строк в начале/конце
4. Пересоздайте секрет в GitHub

### ❌ Ошибка: "Permission denied to Google Sheets"

**Проблема**: Service Account не имеет доступа к таблице

**Решение**:
1. Откройте Google Sheets документ
2. Нажмите "Поделиться"
3. Добавьте email Service Account (из JSON файла, поле `client_email`)
4. Дайте права "Редактор"

### ❌ Ошибка: "Invalid API key"

**Проблема**: Неправильный токен Wildberries

**Решение**:
1. Проверьте токен в личном кабинете WB
2. Убедитесь, что токен активен
3. Обновите секрет `WILDBERRIES_API_KEY` в GitHub

### ❌ Ошибка: "Sheet not found"

**Проблема**: Неправильный ID таблицы или название листа

**Решение**:
1. Проверьте `GOOGLE_SHEET_ID` — это часть URL между `/d/` и `/edit`
2. Проверьте `GOOGLE_SHEET_NAME` — название должно точно совпадать с листом
3. По умолчанию используется "Stock Tracker"

### ⏱️ Workflow не запускается по расписанию

**Причины**:
1. GitHub Actions отключены в настройках репозитория
2. Репозиторий был неактивен более 60 дней (для приватных)
3. Неправильный формат cron

**Решение**:
1. Проверьте: Settings → Actions → General → "Allow all actions"
2. Запустите workflow вручную чтобы активировать
3. Проверьте формат cron в `.github/workflows/update-stocks.yml`

### 📊 Данные не обновляются в таблице

**Проблема**: Скрипт работает, но таблица не меняется

**Решение**:
1. Проверьте логи в GitHub Actions — есть ли сообщения об ошибках
2. Убедитесь, что API Wildberries возвращает данные
3. Проверьте, что Service Account имеет права на редактирование
4. Попробуйте запустить скрипт локально для отладки

---

## 🎛️ Дополнительные настройки

### Изменение расписания

Откройте `.github/workflows/update-stocks.yml` и измените строку cron:

```yaml
schedule:
  # Текущее: каждый день в 00:01 МСК (21:01 UTC)
  - cron: '1 21 * * *'
  
  # Каждый час:
  - cron: '0 * * * *'
  
  # Каждые 6 часов:
  - cron: '0 */6 * * *'
  
  # В 9:00 и 18:00 МСК:
  - cron: '0 6,15 * * *'
  
  # Только по рабочим дням в 9:00 МСК:
  - cron: '0 6 * * 1-5'
```

**⚠️ Важно**: GitHub использует UTC время. МСК = UTC+3.

### Добавление уведомлений в Telegram

Можно настроить отправку уведомлений при успешном обновлении или ошибках.

Добавьте в `.github/workflows/update-stocks.yml`:

```yaml
    - name: 📱 Send Telegram notification
      if: always()
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TELEGRAM_CHAT_ID }}
        token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        message: |
          ${{ job.status == 'success' && '✅' || '❌' }} Stock Tracker Update
          Status: ${{ job.status }}
          Time: ${{ github.event.repository.updated_at }}
```

И добавьте секреты:
- `TELEGRAM_BOT_TOKEN` — токен бота (получить у @BotFather)
- `TELEGRAM_CHAT_ID` — ID чата (можно узнать у @userinfobot)

---

## 📊 Мониторинг

### Просмотр истории запусков

1. Перейдите в **Actions** на GitHub
2. Выберите **"Update Stock Tracker Daily"**
3. Увидите список всех запусков с датой и статусом

### Просмотр логов

1. Кликните на любой запуск
2. Откроется страница с детальными логами
3. Можно развернуть каждый шаг и посмотреть вывод

### Статистика использования

- Перейдите в Settings → Billing
- Увидите использованные минуты GitHub Actions
- Для публичных репозиториев — бесплатно и неограниченно
- Для приватных — 2000 минут/месяц бесплатно

---

## 🔒 Безопасность

✅ **Все секреты зашифрованы** — GitHub использует шифрование для хранения
✅ **Не выводятся в логах** — секреты автоматически скрыты
✅ **Доступны только workflow** — не доступны через API или UI
✅ **Можно ротировать** — легко обновить в любой момент

**Рекомендации**:
- Не коммитьте `config/service-account.json` в git
- Добавьте в `.gitignore`:
```
config/service-account.json
config/credentials.json
.env
*.secret
```

---

## 💡 Полезные ссылки

- [GitHub Actions документация](https://docs.github.com/en/actions)
- [Cron выражения](https://crontab.guru/)
- [Google Cloud Console](https://console.cloud.google.com/)
- [Google Sheets API](https://developers.google.com/sheets/api)
- [Wildberries API документация](https://openapi.wildberries.ru/)

---

## 🎉 Готово!

Теперь ваша таблица будет автоматически обновляться каждый день в **00:01 по МСК** без участия вашего компьютера!

Если возникли вопросы или проблемы — проверьте раздел [Устранение проблем](#устранение-проблем) или создайте Issue в репозитории.

**Успешной автоматизации! 🚀**
