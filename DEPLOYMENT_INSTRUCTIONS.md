# ============================================
# Быстрый Деплой: Пошаговая Инструкция
# ============================================

## ✅ Что Сделано

1. **Код закоммичен и запушен на GitHub** ✅
   - Commit: d0c871d
   - Branch: main
   - 61 файл изменен, 8948 добавлений

2. **Скрипт деплоя загружен на сервер** ✅
   - Файл: deploy_subscription_update.sh
   - Сервер: 158.160.188.247

3. **Проект склонирован на сервер** ✅
   - Директория: ~/Stock-Tracker

---

## 🔧 Что Нужно Сделать Вручную

### Шаг 1: Подключитесь к серверу

```bash
ssh yc-user@158.160.188.247
```

---

### Шаг 2: Создайте .env файл

```bash
cd Stock-Tracker
nano .env
```

**Вставьте следующее содержимое:**

```env
# ============================================
# Database Configuration
# ============================================
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/tgstock
# ИЛИ используйте компоненты:
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tgstock
DB_USER=postgres
DB_PASSWORD=your_password_here

# ============================================
# Telegram Bot
# ============================================
BOT_TOKEN=8558236991:AAHFu2krkBMIWFKF6W_MkIYoIFbfw-d1kms

# ============================================
# Google Sheets
# ============================================
GOOGLE_SHEET_URL=your_sheet_url_here
GOOGLE_DRIVE_FOLDER_ID=your_folder_id_here

# ============================================
# Subscription Configuration (НОВОЕ!)
# ============================================
PAYMENT_ENABLED=false  # false = MVP (бесплатно для всех)
PAYMENT_PROVIDER=yookassa
PAYMENT_TOKEN=  # Оставьте пустым пока

# Настройки подписки
FREE_TRIAL_DAYS=7
SUBSCRIPTION_PRICE=299

# ============================================
# Admin
# ============================================
ADMIN_IDS=your_telegram_id

# ============================================
# Logging
# ============================================
LOG_LEVEL=INFO
```

**Сохраните файл:**
- `Ctrl+O` → Enter → `Ctrl+X`

---

### Шаг 3: Замените значения в .env

Отредактируйте следующие поля:

```bash
nano .env
```

**Обязательно замените:**
1. `DB_PASSWORD=your_password_here` → ваш пароль PostgreSQL
2. `GOOGLE_SHEET_URL=...` → URL вашей таблицы
3. `GOOGLE_DRIVE_FOLDER_ID=...` → ID папки Google Drive
4. `ADMIN_IDS=...` → ваш Telegram ID

---

### Шаг 4: Запустите деплой

```bash
chmod +x deploy_subscription_update.sh
./deploy_subscription_update.sh
```

**Скрипт автоматически:**
- Установит зависимости
- Создаст backup БД
- Применит миграции
- Перезапустит бота

---

### Шаг 5: Проверьте работу

```bash
# Проверьте статус бота
tail -f logs/bot.log

# ИЛИ через systemd (если настроен)
sudo systemctl status stock-tracker-bot

# Проверьте БД
cd Stock-Tracker
source venv/bin/activate
python3 << EOF
from sqlalchemy import create_engine, inspect
import os
from dotenv import load_dotenv

load_dotenv()
db_url = os.getenv('DATABASE_URL')
engine = create_engine(db_url)
inspector = inspect(engine)

print("Таблицы в БД:")
for table in inspector.get_table_names():
    print(f"  • {table}")

if 'subscriptions' in inspector.get_table_names():
    print("\n✅ Таблица subscriptions создана!")
else:
    print("\n❌ Таблица subscriptions отсутствует")
EOF
```

---

## 🎯 Проверка Работы Бота

### Тест 1: Регистрация

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Пройдите регистрацию (имя, email, телефон)
4. Должна прийти ссылка на Google Sheet

**Ожидаемое поведение:**
- Создается запись в `users`
- Создается запись в `subscriptions` с `status='FREE'` и `has_access=True`
- Пользователь получает доступ сразу (бесплатно)

---

### Тест 2: Существующий Пользователь

1. Отправьте `/start`
2. Должно показать главное меню

**Ожидаемое поведение:**
- `check_user_access()` возвращает `True` (payment_enabled=False)
- Доступ к функциям без ограничений

---

### Тест 3: Доступ к Таблице

1. Нажмите кнопку "Получить таблицу снова"
2. Должна прийти ссылка

**Ожидаемое поведение:**
- Проверка через `check_user_access()`
- Успешная отправка ссылки

---

## 📊 Проверка БД

```bash
# Подключитесь к PostgreSQL
psql -U postgres -d tgstock

# Проверьте таблицу subscriptions
SELECT id, user_id, status, has_access FROM subscriptions LIMIT 5;

# Должно показать записи с:
# - status = 'FREE'
# - has_access = TRUE

# Проверьте количество подписок
SELECT status, COUNT(*) FROM subscriptions GROUP BY status;

# Выход
\q
```

---

## 🚀 Включение Платной Модели (В Будущем)

Когда захотите включить платежи:

```bash
cd Stock-Tracker
nano .env
```

**Измените:**
```env
PAYMENT_ENABLED=true
PAYMENT_TOKEN=ваш_yookassa_token
```

**Перезапустите бота:**
```bash
sudo systemctl restart stock-tracker-bot
# ИЛИ
pkill -f "python -m app.main"
cd telegram-bot
nohup python -m app.main > ../logs/bot.log 2>&1 &
```

**Что произойдет:**
- Новые пользователи → TRIAL (7 дней бесплатно)
- Существующие → FREE (бесплатно навсегда)
- После триала → требуется оплата

---

## 🔧 Откат (Если Что-то Пошло Не Так)

```bash
cd Stock-Tracker

# 1. Откатить миграции
source venv/bin/activate
alembic downgrade -1  # Откатить последнюю
alembic downgrade -2  # Откатить две последние

# 2. Восстановить БД из backup
psql -U postgres tgstock < backups/db_backup_YYYYMMDD_HHMMSS.sql

# 3. Откатить код
git checkout HEAD~1  # Предыдущий коммит
```

---

## 📚 Дополнительная Информация

### Логи

```bash
# Логи бота
tail -f logs/bot.log

# Логи PostgreSQL
sudo tail -f /var/log/postgresql/postgresql-*.log

# Логи systemd (если используется)
sudo journalctl -u stock-tracker-bot -f
```

---

### Мониторинг

```bash
# Проверка работы бота через API
BOT_TOKEN=$(grep BOT_TOKEN .env | cut -d '=' -f2)
curl "https://api.telegram.org/bot${BOT_TOKEN}/getMe"

# Должен вернуть:
# {"ok":true,"result":{"id":...,"username":"..."}}
```

---

### Полезные Команды

```bash
# Проверить статус миграций
alembic current
alembic history

# Создать новую миграцию (если нужно)
alembic revision -m "описание"

# Применить конкретную миграцию
alembic upgrade <revision_id>

# Проверить подключение к БД
psql -U postgres -d tgstock -c "SELECT version();"
```

---

## ✅ Готово!

После выполнения всех шагов:

1. ✅ Код обновлен на сервере
2. ✅ Миграции применены
3. ✅ Бот запущен
4. ✅ Система подписок работает

**Текущий режим:** MVP (бесплатно для всех)  
**Готовность к платежам:** 100% (одна строка в .env)

---

## 🆘 Если Нужна Помощь

1. Проверьте логи: `tail -f logs/bot.log`
2. Проверьте БД: `psql -U postgres -d tgstock`
3. Проверьте .env: `cat .env | grep -v "PASSWORD\|TOKEN"`

**Документация:**
- [HOW_BOT_WORKS.md](HOW_BOT_WORKS.md) - Описание работы
- [SUBSCRIPTION_ARCHITECTURE.md](SUBSCRIPTION_ARCHITECTURE.md) - Архитектура
- [CODE_REVIEW_SUMMARY.md](CODE_REVIEW_SUMMARY.md) - Проверка кода
