# Отчёт об исправлении расхождений данных
**Дата:** 09.11.2025  
**Анализ:** Детальное сравнение официальных данных WB с таблицей Stock Tracker

## 🔍 Выявленные проблемы

### Проблема #1: Избыточные заказы на складе "Маркетплейс"
**Симптомы:**
- Its1_2_3/50g: официально 3 заказа → в таблице 16 (5.3x больше)
- ItsSport2/50g: официально 1 заказ → в таблице 14 (14x больше)
- Its2/50g: данные совпадают (3 = 3)

**Корневая причина:**
Неправильная нормализация названий складов в `warehouse_mapper.py`:
```python
# БЫЛО (НЕПРАВИЛЬНО):
"Подольск": ["Подольск 3", "Подольск-3", ...]  # "Подольск 3" → "Подольск"
"Чехов": ["Чехов 1", "Чехов-1", ...]           # "Чехов 1" → "Чехов"
```

Заказы со складов "Подольск 3" (9 заказов) и "Екатеринбург - Перспективный 12" (13 заказов) ошибочно объединялись в "Подольск", а затем частично попадали в "Маркетплейс" из-за агрессивного поиска по ключевым словам "мп".

**Исправление:**
```python
# СТАЛО (ПРАВИЛЬНО):
"Подольск 3": ["Подольск 3", "Подольск-3", ...]  # Отдельный склад
"Чехов 1": ["Чехов 1", "Чехов-1", ...]           # Отдельный склад
"Екатеринбург - Перспективный 12": [...]          # Добавлен как отдельный склад
```

### Проблема #2: Название склада "Чехов 1" отображается как "1" или "Чехов"
**Симптомы:**
- В официальных данных: "Чехов 1" (212 остатков для Its2/50g)
- В таблице: либо "1", либо остатки перемещены в "Чехов"

**Корневая причина:**
Та же проблема неправильной нормализации - "Чехов 1" преобразовывался в "Чехов" через маппинг.

**Исправление:**
Исправлено через обновление `WAREHOUSE_NAME_MAPPINGS` - теперь "Чехов 1" сохраняется как отдельное каноническое название.

### Проблема #3: Устаревшие остатки на складах с нулевыми данными
**Симптомы:**
- Коледино: в таблице 31-39 остатков, официально 0
- Обухово: в таблице 1-3 остатка, официально 0
- Подольск: в таблице 2-18 остатков, официально 0

**Корневая причина:**
Код включал склады с `quantity=0` из API (правильно для текущих данных), но не очищал склады, которые вообще исчезли из API. Старые данные оставались в таблице.

**Исправление:**
В `product_service.py` добавлена фильтрация:
```python
# Пропускаем склады без остатков И без заказов (устаревшие данные)
if stock == 0 and orders == 0:
    logger.debug(f"Skipping warehouse {wh_name} with zero stock and zero orders")
    continue
```

### Проблема #4: Агрессивная детекция "Маркетплейс" перед точными названиями
**Симптомы:**
Региональные склады типа "Обухово МП" могли ошибочно нормализоваться в "Маркетплейс".

**Корневая причина:**
В `_find_partial_match()` сначала проверялись короткие ключевые слова "мп", "сп" перед точным поиском по полным названиям складов.

**Исправление:**
Изменён порядок приоритетов в `_find_partial_match()`:
1. **ПРИОРИТЕТ #1:** Полные совпадения с вариантами складов (например, "Подольск 3")
2. **ПРИОРИТЕТ #2:** Явные индикаторы "Маркетплейс" (без коротких "мп", "сп")
3. **ПРИОРИТЕТ #3:** Частичное совпадение для обычных складов

## ✅ Применённые исправления

### 1. Файл: `src/stock_tracker/utils/warehouse_mapper.py`

**Строки 17-25:** Исправлены маппинги складов
```python
WAREHOUSE_NAME_MAPPINGS = {
    # ИСПРАВЛЕНО 09.11.2025: Удалены неправильные маппинги
    "Новосемейкино": ["Самара (Новосемейкино)", ...],
    "Чехов 1": ["Чехов 1", "Чехов-1", ...],  # Отдельный склад
    "Подольск 3": ["Подольск 3", "Подольск-3", ...],  # Отдельный склад
    "Екатеринбург - Перспективный 12": [...],  # Добавлен
    ...
}
```

**Строки 126-175:** Изменена логика `_find_partial_match()`
- Убрана агрессивная приоритетная проверка коротких маркеров "мп", "сп"
- Добавлен приоритет #1: полные совпадения складов
- Маркетплейс теперь детектируется только по явным индикаторам

### 2. Файл: `src/stock_tracker/services/product_service.py`

**Строки 584-606:** Добавлена фильтрация устаревших складов
```python
# Create Warehouse objects
# ИСПРАВЛЕНО 09.11.2025: Не добавляем склады с нулевыми остатками И нулевыми заказами
for wh_name, stock in warehouse_stocks.items():
    orders = warehouse_orders.get(wh_name, 0)
    # Пропускаем склады без остатков и заказов (устаревшие данные)
    if stock == 0 and orders == 0:
        logger.debug(f"Skipping warehouse {wh_name} with zero stock and zero orders")
        continue
    
    warehouse = Warehouse(name=wh_name, stock=stock, orders=orders)
    warehouses.append(warehouse)
```

## 📊 Результаты сравнения

### Its2/50g
- **Заказы:** 75 (официально) vs 81 (таблица) — разница **6 заказов**
  - Критические расхождения: Подольск (+7), Подольск 3 (-9)
- **Остатки:** 2045 (официально) vs 1889 (таблица) — разница **-156 остатков**
  - Критические расхождения: Чехов 1 (-212)

### Its1_2_3/50g
- **Заказы:** 74 (официально) vs 76 (таблица) — разница **2 заказа**
  - Критические расхождения: Маркетплейс (+13), Екатеринбург -Перспективный 12 (-13)
- **Остатки:** 3298 (официально) vs 3349 (таблица) — разница **51 остаток**
  - Критические расхождения: Маркетплейс (+191), Чехов (+192), Чехов 1 (-192)

### ItsSport2/50g
- **Заказы:** 44 (официально) vs 47 (таблица) — разница **3 заказа**
  - Критические расхождения: Маркетплейс (+13), Подольск (+8), Подольск 3 (-8), Екатеринбург -Перспективный 12 (-13)
- **Остатки:** 1111 (официально) vs 1045 (таблица) — разница **-66 остатков**
  - Критические расхождения: Чехов 1 (-86)

## 🎯 Ожидаемый эффект от исправлений

После обновления таблицы с новыми исправлениями:

1. **Склады с номерами сохранятся:**
   - "Подольск 3" останется "Подольск 3" (не превратится в "Подольск")
   - "Чехов 1" останется "Чехов 1" (не превратится в "Чехов" или "1")
   - "Екатеринбург - Перспективный 12" будет корректно отображаться

2. **Заказы "Маркетплейс" станут точными:**
   - Its1_2_3/50g: 16 → 3 заказа (убраны лишние 13 из Екатеринбурга)
   - ItsSport2/50g: 14 → 1 заказ (убраны лишние 13 из Екатеринбурга)

3. **Устаревшие склады исчезнут:**
   - Коледино (0/0) — удалён
   - Обухово (0/0) — удалён
   - Подольск с нулями — удалён

4. **Остатки на "Чехов 1" появятся:**
   - Its2/50g: 0 → 212 остатков
   - Its1_2_3/50g: 0 → 192 остатка
   - ItsSport2/50g: 0 → 86 остатков

## 🚀 Следующие шаги

1. **Запустить обновление таблицы:**
   ```bash
   python update_table_fixed.py
   ```

2. **Проверить результаты:**
   ```bash
   python detailed_product_comparison.py
   ```
   Ожидается, что критические расхождения будут устранены.

3. **Мониторинг:**
   Проверить логи на наличие сообщений:
   - `"✅ EXACT MATCH: 'Подольск 3' -> 'Подольск 3'"`
   - `"✅ EXACT MATCH: 'Чехов 1' -> 'Чехов 1'"`
   - `"Skipping warehouse ... with zero stock and zero orders"`

## 📝 Примечания

- Все исправления обратно совместимы
- Код продолжит правильно работать с существующими данными
- Нормализация применяется только для дедупликации, канонические названия сохраняются
- Склады с заказами но без остатков продолжат отображаться (как и должно быть)
