# Stock Tracker - Автоматизация учёта остатков Wildberries

Система автоматической синхронизации товарных остатков из Wildberries API в Google Sheets с поддержкой FBO/FBS, расчётом оборачиваемости и детализацией по складам.

---

## 🎯 Основные возможности

- ✅ **Dual API подход:** объединение данных из Wildberries API v1 (FBO) и v3 (FBS)
- ✅ **Автоматическая синхронизация:** по расписанию через GitHub Actions или локально
- ✅ **Расчёт оборачиваемости:** turnover rate для каждого товара
- ✅ **Детализация по складам:** раздельный учёт FBO/FBS остатков
- ✅ **Нормализация складов:** единый маппинг названий складов WB
- ✅ **Батчинг и rate limiting:** оптимизация запросов к API
- ✅ **Retry механизм:** устойчивость к временным сбоям
- ✅ **Логирование:** детальные логи всех операций

---

## 📁 Структура проекта

```
Stock-Tracker/
├── src/stock_tracker/        # Основной код
│   ├── core/                  # Модели данных (Product, Warehouse)
│   ├── services/              # Бизнес-логика (ProductService, API fetchers)
│   ├── database/              # Google Sheets интеграция
│   └── utils/                 # Утилиты (config, warehouse_mapper, logging)
├── tests/                     # Unit и integration тесты
├── archive/                   # Архивные/вспомогательные скрипты
│   ├── analysis/              # Аналитические и сравнительные
│   ├── checks/                # Проверки структуры данных
│   ├── debug/                 # Отладка и диагностика
│   ├── fixes/                 # Одноразовые исправления
│   ├── verification/          # Финальные верификации
│   ├── tests/                 # Нестандартные тесты
│   ├── demo/                  # Демонстрационные скрипты
│   └── legacy/                # Устаревшие версии
├── docs/                      # Документация
│   └── HISTORY.md             # История разработки
├── tools/                     # Утилиты управления
│   ├── restructure_archive.ps1  # Скрипт реорганизации
│   └── cleanup_artifacts.ps1    # Очистка артефактов
├── update_table_fixed.py      # Основной скрипт синхронизации
├── run_full_sync.py           # Полная синхронизация
├── requirements.txt           # Python зависимости
└── .env.example               # Пример конфигурации
```

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Скопируйте `.env.example` в `.env` и укажите:
```env
WB_API_KEY=ваш_ключ_wildberries
GOOGLE_SHEETS_ID=id_таблицы
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=config/service-account.json
```

### 3. Запуск синхронизации

```powershell
python update_table_fixed.py
```

Или полная синхронизация с детальными логами:
```powershell
python run_full_sync.py
```

---

## 📊 Основные скрипты

| Скрипт | Назначение |
|--------|------------|
| `update_table_fixed.py` | Основная синхронизация (Dual API → Sheets) |
| `run_full_sync.py` | Полная синхронизация с детальным выводом |
| `direct_sync.py` | Простая синхронизация без retry |
| `retry_sync_with_fix.py` | Синхронизация с расширенным retry |

---

## 🧪 Тестирование

Запуск всех unit-тестов:
```powershell
pytest tests/ -v
```

Запуск конкретных тестов:
```powershell
pytest tests/test_orders_counting.py -v
pytest tests/test_performance_analysis.py -v
pytest tests/test_sheets_capacity.py -v
```

---

## 🔄 Автоматизация

### GitHub Actions (рекомендуется)

Синхронизация настроена автоматически через GitHub Actions:
- **Расписание:** каждые 6 часов
- **Конфигурация:** `.github/workflows/sync.yml`
- **Secrets:** добавьте `WB_API_KEY`, `GOOGLE_SHEETS_ID`, `GOOGLE_SERVICE_ACCOUNT_JSON` в настройки репозитория

См. подробности в `GITHUB_ACTIONS_SETUP.md`.

### Локальная автоматизация (Windows)

1. Настройте `.env`:
```env
AUTO_SYNC_ENABLED=true
SYNC_SCHEDULE=0 2 * * *  # Ежедневно в 2:00
```

2. Запустите Windows Task Scheduler или используйте:
```powershell
.\setup_windows_task.ps1
```

---

## 🛠️ Утилиты управления

### Реорганизация архива
```powershell
powershell -ExecutionPolicy Bypass -File tools/restructure_archive.ps1 -DryRun
powershell -ExecutionPolicy Bypass -File tools/restructure_archive.ps1
```

### Очистка артефактов
```powershell
powershell -ExecutionPolicy Bypass -File tools/cleanup_artifacts.ps1 -DryRun
powershell -ExecutionPolicy Bypass -File tools/cleanup_artifacts.ps1
```

---

## 📚 Документация

- **История разработки:** `docs/HISTORY.md`
- **Архивная структура:** `archive/README.md`
- **GitHub Actions:** `GITHUB_ACTIONS_SETUP.md`
- **Быстрая настройка:** `QUICK_SETUP.md`
- **Детальная настройка:** `SETUP_GUIDE.md`

---

## 🧩 Архитектура

### Dual API подход

Система объединяет данные из двух источников:
1. **Warehouse API v1:** FBO остатки (склады WB)
2. **Marketplace API v3:** FBS остатки (склады продавца, маркетплейс)

Данные дедуплицируются по `seller_article` и объединяются с нормализацией складов.

### Нормализация складов

Все варианты имён складов приводятся к единому формату через `warehouse_mapper`:
- "Fulllog FBS" → "Маркетплейс"
- "Новосемейкино" = "Самара (Новосемейкино)" → единая нормализация
- Фильтрация складов "в пути" (не суммируются в остатки)

### Rate Limiting

- **Warehouse API:** ~6 req/min
- **Marketplace API:** 6 req/min (строгий лимит)
- **Батчинг:** до 100 товаров за запрос к Sheets
- **Retry:** экспоненциальный backoff с макс. 3 попытки

---

## ⚙️ Конфигурация

Основные параметры в `.env`:

```env
# API Wildberries
WB_API_KEY=eyJhbGciOiJFUz...

# Google Sheets
GOOGLE_SHEETS_ID=1abc...xyz
GOOGLE_SHEET_NAME=Stock Tracker
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=config/service-account.json

# Автосинхронизация
AUTO_SYNC_ENABLED=true
SYNC_SCHEDULE=0 */6 * * *

# Rate Limiting
WB_API_RATE_LIMIT=6
WB_API_RATE_PERIOD=60
```

---

## 🐛 Отладка и диагностика

### Логи

Все операции логируются в `logs/stock_tracker.log`:
```powershell
Get-Content logs/stock_tracker.log -Tail 50
```

### Проверка квот API
```powershell
python archive/analysis/analyze_api_quota_usage.py
```

### Проверка данных
```powershell
python archive/checks/check_current_api_state.py
```

---

## 🔧 Известные ограничения

1. **Wildberries API v1:** не всегда возвращает FBS данные для всех артикулов
2. **Marketplace API:** жёсткий лимит 6 req/min требует батчинга больших каталогов
3. **CSV выгрузки WB:** могут отличаться от API (разные источники)
4. **Google Sheets API:** квота 100 req/100s, используется батчинг

---

## 📈 Метрики и мониторинг

Система рассчитывает:
- **Turnover Rate:** `orders / stock` (оборачиваемость)
- **Performance Category:** high/medium/low по оборачиваемости
- **Stock Status:** adequate / low_stock / out_of_stock
- **Risk Level:** low / medium / high / critical

---

## 🤝 Вклад и разработка

1. Форкните репозиторий
2. Создайте feature-ветку: `git checkout -b feature/my-feature`
3. Коммитьте изменения: `git commit -m "feat: add feature"`
4. Пушьте в ветку: `git push origin feature/my-feature`
5. Откройте Pull Request

### Запуск тестов перед PR
```powershell
pytest tests/ -v --cov=src
```

---

## 📞 Поддержка

- **Issues:** [GitHub Issues](https://github.com/ameba7464/Stock-Tracker-/issues)
- **Discussions:** используйте GitHub Discussions для вопросов
- **Email:** см. профиль владельца репозитория

---

## 📝 Лицензия

Проект распространяется по лицензии MIT. См. `LICENSE` для деталей.

---

## 🎯 Статус проекта

✅ **Стабильный релиз** — все критические проблемы устранены, система работает в продакшене.

**Последнее обновление:** 15 ноября 2025

---

_Если у вас остались вопросы — см. `docs/HISTORY.md` с детальной историей разработки и решённых проблем._
