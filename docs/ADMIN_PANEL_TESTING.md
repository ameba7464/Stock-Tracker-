# 🧪 Тестирование админ-панели

## Подготовка к тестированию

### 1. Проверьте зависимости

Убедитесь, что все необходимые пакеты установлены:

```powershell
pip list | Select-String "fastapi|sqlalchemy|pydantic"
```

Если чего-то не хватает:
```powershell
pip install -r requirements.txt
```

### 2. Проверьте базу данных

Убедитесь, что PostgreSQL запущен и доступен:

```powershell
# Проверьте подключение
python -c "from stock_tracker.database.connection import engine; print('✅ Database connected' if engine else '❌ Connection failed')"
```

### 3. Примените миграции

```powershell
alembic upgrade head
```

Ожидаемый результат:
```
INFO  [alembic.runtime.migration] Running upgrade -> add_is_admin_field
```

## Пошаговое тестирование

### Тест 1: Создание администратора

```powershell
# Замените на ваш email
python scripts/create_admin.py grant admin@test.com
```

Ожидаемый результат:
```
✅ Successfully granted admin privileges to 'admin@test.com'

📋 User details:
   - ID: <uuid>
   - Email: admin@test.com
   - Name: ...
   - Role: ...
   - Is Admin: True
   - Is Active: True

🌐 Access admin panel at: http://localhost:8000/admin/admin.html
```

### Тест 2: Список администраторов

```powershell
python scripts/create_admin.py list
```

Должны увидеть список всех админов.

### Тест 3: Запуск приложения

```powershell
python main.py
```

Или:
```powershell
uvicorn stock_tracker.api.main:app --reload --host 0.0.0.0 --port 8000
```

Ожидаемый вывод:
```
INFO:     🚀 Starting Stock Tracker API...
INFO:     ✅ Sentry initialized
INFO:     ✅ Prometheus metrics initialized
INFO:     ✅ Admin panel mounted at /admin (static files from ...)
INFO:     ✅ API started successfully
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### Тест 4: Доступ к API эндпоинтам

Откройте новый терминал PowerShell:

```powershell
# Сначала войдите и получите токен
$loginData = @{
    email = "admin@test.com"
    password = "ваш_пароль"
} | ConvertTo-Json

$response = Invoke-WebRequest -Uri "http://localhost:8000/api/v1/auth/login" `
    -Method POST `
    -ContentType "application/json" `
    -Body $loginData

$token = ($response.Content | ConvertFrom-Json).access_token

# Проверьте доступ к статистике
$headers = @{
    Authorization = "Bearer $token"
}

Invoke-WebRequest -Uri "http://localhost:8000/api/v1/admin/stats" -Headers $headers
```

Должны получить JSON с статистикой.

### Тест 5: Веб-интерфейс

1. Откройте браузер
2. Перейдите на http://localhost:8000/admin/login.html
3. Введите email и пароль администратора
4. После входа должны увидеть админ-панель с:
   - Карточками статистики
   - Таблицей пользователей
   - Поиском и фильтрами

### Тест 6: Функции админ-панели

#### 6.1. Поиск пользователей
- Введите email или имя в поле поиска
- Таблица должна отфильтроваться автоматически

#### 6.2. Фильтрация по тарифу
- Выберите тариф в выпадающем списке
- Должны отобразиться только пользователи с выбранным тарифом

#### 6.3. Просмотр деталей пользователя
- Кликните на любого пользователя в таблице
- Должно открыться модальное окно с полной информацией

#### 6.4. Изменение прав администратора
- В модальном окне нажмите "Выдать Admin"
- Подтвердите действие
- Пользователь должен получить права админа

#### 6.5. Изменение тарифа
- Нажмите "Изменить тариф"
- Введите новый тариф (например, "pro")
- Тариф пользователя должен измениться

#### 6.6. Деактивация пользователя
- Нажмите "Деактивировать"
- Подтвердите действие
- Пользователь должен стать неактивным

## Проверка логов

Во время тестирования проверяйте логи приложения:

```powershell
# В терминале, где запущено приложение, вы должны видеть:
INFO:     Admin <email> set user <email> active status to False
INFO:     Admin <email> set user <email> admin status to True
INFO:     Admin <email> changed user <email> plan from free to pro
```

## Тестирование безопасности

### Тест 7: Доступ без токена

```powershell
# Попытка доступа без токена
Invoke-WebRequest -Uri "http://localhost:8000/api/v1/admin/stats"
```

Ожидаемый результат: **401 Unauthorized**

### Тест 8: Доступ обычного пользователя

1. Создайте обычного пользователя (без прав админа)
2. Войдите под ним
3. Попытайтесь открыть /admin/admin.html

Ожидаемый результат: **403 Forbidden** при попытке загрузить данные

### Тест 9: Защита от самоудаления

1. Войдите как администратор
2. Найдите свой аккаунт в списке
3. Попытайтесь забрать у себя права админа
4. Попытайтесь удалить свой аккаунт

Ожидаемый результат: Ошибка с сообщением о невозможности действия

## Тестирование производительности

### Тест 10: Нагрузка на API

```powershell
# Используйте Apache Bench (если установлен)
ab -n 100 -c 10 -H "Authorization: Bearer $token" http://localhost:8000/api/v1/admin/stats
```

Или напишите простой скрипт:

```powershell
$token = "ваш_токен"
$headers = @{Authorization = "Bearer $token"}

1..50 | ForEach-Object {
    $start = Get-Date
    Invoke-WebRequest -Uri "http://localhost:8000/api/v1/admin/users?page=$_" -Headers $headers
    $end = Get-Date
    $duration = ($end - $start).TotalMilliseconds
    Write-Host "Request $_: ${duration}ms"
}
```

## Решение проблем

### Проблема: Миграция не применяется

**Решение:**
```powershell
# Проверьте текущую версию
alembic current

# Посмотрите историю
alembic history

# Примените принудительно
alembic upgrade head --sql  # Просмотр SQL
alembic upgrade head  # Применить
```

### Проблема: Статические файлы не загружаются

**Решение:**
```powershell
# Проверьте, что файлы существуют
Test-Path "src\stock_tracker\static\admin.html"
Test-Path "src\stock_tracker\static\login.html"

# Перезапустите приложение
```

### Проблема: CORS ошибки

**Решение:**
Обновите `.env`:
```
CORS_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
```

### Проблема: База данных не отвечает

**Решение:**
```powershell
# Проверьте статус PostgreSQL
Get-Service postgresql*

# Перезапустите если нужно
Restart-Service postgresql-x64-14  # замените на вашу версию
```

## Чек-лист перед продакшеном

- [ ] Миграция успешно применена
- [ ] Все тесты прошли успешно
- [ ] Логи не содержат ошибок
- [ ] HTTPS настроен (в продакшене)
- [ ] CORS правильно настроен
- [ ] Сильные пароли для админов
- [ ] Резервное копирование базы данных настроено
- [ ] Мониторинг и алерты настроены
- [ ] Rate limiting работает корректно
- [ ] Статические файлы сжаты и оптимизированы

## Автоматизированное тестирование

Создайте файл `test_admin_panel.py`:

```python
import pytest
from fastapi.testclient import TestClient
from stock_tracker.api.main import app

client = TestClient(app)

def test_admin_login():
    response = client.post(
        "/api/v1/auth/login",
        json={"email": "admin@test.com", "password": "test123"}
    )
    assert response.status_code == 200
    assert "access_token" in response.json()

def test_admin_stats():
    # Сначала получить токен
    login_response = client.post(
        "/api/v1/auth/login",
        json={"email": "admin@test.com", "password": "test123"}
    )
    token = login_response.json()["access_token"]
    
    # Запросить статистику
    response = client.get(
        "/api/v1/admin/stats",
        headers={"Authorization": f"Bearer {token}"}
    )
    assert response.status_code == 200
    data = response.json()
    assert "total_users" in data
    assert "active_users" in data

def test_non_admin_access():
    # Войти как обычный пользователь
    login_response = client.post(
        "/api/v1/auth/login",
        json={"email": "user@test.com", "password": "test123"}
    )
    token = login_response.json()["access_token"]
    
    # Попытаться получить доступ к админке
    response = client.get(
        "/api/v1/admin/stats",
        headers={"Authorization": f"Bearer {token}"}
    )
    assert response.status_code == 403
```

Запустите тесты:
```powershell
pytest test_admin_panel.py -v
```

## Финальная проверка

После всех тестов выполните:

```powershell
# 1. Проверьте здоровье приложения
Invoke-WebRequest -Uri "http://localhost:8000/api/v1/health"

# 2. Проверьте метрики
Invoke-WebRequest -Uri "http://localhost:8000/metrics"

# 3. Проверьте документацию
Start-Process "http://localhost:8000/docs"

# 4. Проверьте админ-панель
Start-Process "http://localhost:8000/admin/login.html"
```

Если все работает - поздравляю! Ваша админ-панель готова к использованию! 🎉
