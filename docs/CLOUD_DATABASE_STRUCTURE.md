# 📊 Unified Cloud Database Structure

## Обзор

Единая база данных Stock Tracker объединяет функционал Telegram бота и FastAPI Admin Panel в облаке Yandex Cloud.

**Подключение:**
```
Host: 158.160.188.247
Port: 5432
Database: stocktracker
User: stocktracker
Password: StockTracker2024
```

**Connection String:**
```
postgresql+psycopg://stocktracker:StockTracker2024@158.160.188.247:5432/stocktracker
```

---

## 🗂️ Структура базы данных

### 1. **tenants** - Мультитенантность
Организации/компании, использующие систему.

| Колонка | Тип | Описание |
|---------|-----|----------|
| id | UUID | Уникальный идентификатор |
| name | VARCHAR(255) | Название организации |
| slug | VARCHAR(100) | URL-friendly идентификатор |
| is_active | BOOLEAN | Активна ли организация |
| created_at | TIMESTAMP | Дата создания |
| updated_at | TIMESTAMP | Дата обновления |

---

### 2. **users** - Унифицированные пользователи
Объединяет пользователей Telegram бота и админ панели.

| Колонка | Тип | Описание |
|---------|-----|----------|
| **Идентификация** | | |
| id | UUID | Уникальный идентификатор |
| email | VARCHAR(255) | Email (уникальный) |
| password_hash | VARCHAR(255) | Хеш пароля (NULL для Telegram-only) |
| **Telegram** | | |
| telegram_id | BIGINT | ID пользователя в Telegram |
| telegram_username | VARCHAR(100) | Username в Telegram |
| **Профиль** | | |
| full_name | VARCHAR(255) | Полное имя |
| phone | VARCHAR(20) | Телефон |
| **Wildberries** | | |
| wb_api_key | TEXT | API ключ Wildberries |
| wb_api_key_encrypted | TEXT | Зашифрованный API ключ |
| **Google Sheets** | | |
| google_sheet_id | VARCHAR(255) | ID Google таблицы |
| google_sheet_sent | BOOLEAN | Отправлена ли таблица |
| **Доступ** | | |
| tenant_id | UUID | Организация пользователя |
| role | ENUM | Роль: OWNER, ADMIN, USER, VIEWER |
| is_active | BOOLEAN | Активен ли аккаунт |
| is_verified | BOOLEAN | Подтверждён ли email |
| is_admin | BOOLEAN | Суперадминистратор |
| **Подписка** | | |
| payment_status | ENUM | free, trial, paid, expired |
| subscription_expires_at | TIMESTAMP | Окончание подписки |
| trial_ends_at | TIMESTAMP | Окончание триала |
| **Активность** | | |
| last_login_at | TIMESTAMP | Последний вход |
| last_sync_at | TIMESTAMP | Последняя синхронизация |
| created_at | TIMESTAMP | Дата регистрации |
| updated_at | TIMESTAMP | Дата обновления |

**Индексы:**
- `ix_users_email` - по email
- `ix_users_telegram_id` - по telegram_id
- `ix_users_is_admin` - по is_admin
- `ix_users_payment_status` - по статусу оплаты
- `ix_users_tenant_role` - составной по tenant_id + role

---

### 3. **subscriptions** - Подписки
История и активные подписки пользователей.

| Колонка | Тип | Описание |
|---------|-----|----------|
| id | UUID | Уникальный идентификатор |
| user_id | UUID | Пользователь |
| plan_name | VARCHAR(50) | Название тарифа |
| status | ENUM | trial, active, expired, cancelled |
| price | DECIMAL(10,2) | Цена |
| currency | VARCHAR(3) | Валюта (RUB) |
| started_at | TIMESTAMP | Начало подписки |
| expires_at | TIMESTAMP | Окончание подписки |
| cancelled_at | TIMESTAMP | Дата отмены |
| auto_renew | BOOLEAN | Автопродление |

---

### 4. **products** - Товары Wildberries
Товары пользователей из Wildberries.

| Колонка | Тип | Описание |
|---------|-----|----------|
| id | UUID | Уникальный идентификатор |
| user_id | UUID | Владелец товара |
| wb_article | VARCHAR(100) | Артикул WB |
| wb_nm_id | BIGINT | Номенклатурный номер WB |
| name | VARCHAR(500) | Название товара |
| brand | VARCHAR(255) | Бренд |
| category | VARCHAR(255) | Категория |
| price | DECIMAL(10,2) | Цена |
| discount | INTEGER | Скидка (%) |
| rating | DECIMAL(3,2) | Рейтинг |
| reviews_count | INTEGER | Количество отзывов |
| in_stock | BOOLEAN | В наличии |
| last_sync_at | TIMESTAMP | Последняя синхронизация |

---

### 5. **stock_history** - История остатков
История изменения остатков товаров по складам.

| Колонка | Тип | Описание |
|---------|-----|----------|
| id | UUID | Уникальный идентификатор |
| product_id | UUID | Товар |
| warehouse_name | VARCHAR(255) | Название склада |
| quantity | INTEGER | Количество |
| recorded_at | TIMESTAMP | Время записи |

---

### 6. **sales** - Продажи
История продаж товаров.

| Колонка | Тип | Описание |
|---------|-----|----------|
| id | UUID | Уникальный идентификатор |
| user_id | UUID | Продавец |
| product_id | UUID | Товар |
| wb_order_id | VARCHAR(100) | ID заказа WB |
| sale_date | DATE | Дата продажи |
| quantity | INTEGER | Количество |
| price | DECIMAL(10,2) | Цена продажи |
| commission | DECIMAL(10,2) | Комиссия WB |
| net_amount | DECIMAL(10,2) | Чистая прибыль |

---

### 7. **analytics_cache** - Кеш аналитики
Предрассчитанные метрики для быстрого отображения.

| Колонка | Тип | Описание |
|---------|-----|----------|
| id | UUID | Уникальный идентификатор |
| user_id | UUID | Пользователь |
| metric_type | VARCHAR(50) | Тип метрики |
| period | VARCHAR(20) | Период (daily, weekly, monthly) |
| data | JSONB | Данные метрики |
| calculated_at | TIMESTAMP | Время расчёта |
| expires_at | TIMESTAMP | Время истечения |

---

### 8. **audit_log** - Журнал аудита
Лог всех действий администраторов и важных операций.

| Колонка | Тип | Описание |
|---------|-----|----------|
| id | UUID | Уникальный идентификатор |
| user_id | UUID | Пользователь (объект действия) |
| admin_user_id | UUID | Администратор (субъект действия) |
| action | VARCHAR(100) | Действие |
| entity_type | VARCHAR(50) | Тип сущности |
| entity_id | UUID | ID сущности |
| changes | JSONB | Изменения |
| ip_address | INET | IP адрес |
| user_agent | TEXT | User Agent |
| created_at | TIMESTAMP | Время действия |

---

### 9. **system_metrics** - Системные метрики
Метрики производительности и мониторинга системы.

| Колонка | Тип | Описание |
|---------|-----|----------|
| id | UUID | Уникальный идентификатор |
| metric_name | VARCHAR(100) | Название метрики |
| metric_value | DECIMAL(15,2) | Значение |
| tags | JSONB | Теги/метаданные |
| recorded_at | TIMESTAMP | Время записи |

---

### 10. **notifications** - Уведомления
Уведомления для пользователей.

| Колонка | Тип | Описание |
|---------|-----|----------|
| id | UUID | Уникальный идентификатор |
| user_id | UUID | Получатель |
| type | VARCHAR(50) | Тип уведомления |
| title | VARCHAR(255) | Заголовок |
| message | TEXT | Сообщение |
| is_read | BOOLEAN | Прочитано |
| data | JSONB | Дополнительные данные |
| created_at | TIMESTAMP | Время создания |

---

### 11. **refresh_tokens** - Refresh токены
JWT refresh токены для длительной авторизации.

| Колонка | Тип | Описание |
|---------|-----|----------|
| id | UUID | Уникальный идентификатор |
| user_id | UUID | Пользователь |
| token | VARCHAR(255) | Токен |
| expires_at | TIMESTAMP | Время истечения |
| created_at | TIMESTAMP | Время создания |

---

## 📈 Views (Представления)

### v_active_users
Активные пользователи с подписками:
```sql
SELECT 
    u.id, u.email, u.telegram_id, u.full_name, u.role, u.is_admin,
    u.payment_status, u.last_login_at,
    t.name as tenant_name,
    s.plan_name, s.expires_at as subscription_expires_at
FROM users u
LEFT JOIN tenants t ON u.tenant_id = t.id
LEFT JOIN subscriptions s ON u.id = s.user_id AND s.status = 'active'
WHERE u.is_active = true;
```

### v_admin_stats
Статистика для дашборда администратора:
```sql
SELECT
    COUNT(*) FILTER (WHERE is_active = true) as total_users,
    COUNT(*) FILTER (WHERE payment_status = 'paid') as paid_users,
    COUNT(*) FILTER (WHERE payment_status = 'trial') as trial_users,
    COUNT(*) FILTER (WHERE is_admin = true) as admin_users,
    (SELECT COUNT(*) FROM products) as total_products,
    (SELECT COUNT(*) FROM sales WHERE sale_date >= CURRENT_DATE - 30) as sales_last_30_days
FROM users;
```

---

## 🔧 Миграция данных

Данные из старой таблицы `users` автоматически мигрированы в новую структуру:

✅ **Мигрировано пользователей:** 2
- miroslavbabenko228@gmail.com (is_admin: true, payment_status: paid)
- klyiiv@duck.com (is_admin: false, payment_status: paid)

Старая таблица сохранена как `users_old_backup` для резервной копии.

---

## 🚀 Подключение из приложений

### Python (SQLAlchemy)
```python
from sqlalchemy import create_engine

DATABASE_URL = "postgresql+psycopg://stocktracker:StockTracker2024@158.160.188.247:5432/stocktracker"
engine = create_engine(DATABASE_URL)
```

### Python (asyncpg для Telegram бота)
```python
import asyncpg

DATABASE_URL = "postgresql+asyncpg://stocktracker:StockTracker2024@158.160.188.247:5432/stocktracker"
```

### FastAPI Admin Panel
```bash
cd Stock-Tracker
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

# Update .env
DATABASE_URL=postgresql+psycopg://stocktracker:StockTracker2024@158.160.188.247:5432/stocktracker

# Run
uvicorn stock_tracker.api.main:app --host 0.0.0.0 --port 8000 --reload
```

### Доступ к админ панели
```
URL: http://localhost:8000/admin/login.html
Email: miroslavbabenko228@gmail.com
Password: asacud (нужно установить через bcrypt)
```

---

## 🔐 Безопасность

1. ✅ Все пароли хешированы через bcrypt
2. ✅ API ключи могут быть зашифрованы через Fernet
3. ✅ JWT токены с коротким временем жизни (15 мин)
4. ✅ Refresh токены для длительных сессий (30 дней)
5. ✅ Audit log для всех административных действий
6. ✅ Role-based access control (RBAC)

---

## 📊 Мониторинг

Система готова к интеграции мониторинга:

1. **System Metrics** - таблица для хранения метрик производительности
2. **Audit Log** - полный лог действий администраторов
3. **Analytics Cache** - кеширование тяжёлых расчётов
4. **Views** - готовые представления для дашбордов

### Планируемые метрики:
- Количество активных пользователей
- Конверсия trial → paid
- Средний чек подписки
- Нагрузка на API
- Время отклика запросов
- Ошибки и исключения

---

## 🔄 Следующие шаги

1. ✅ Структура БД создана
2. ✅ Данные мигрированы
3. ✅ FastAPI модели обновлены
4. ⏳ Обновить эндпоинты админ панели
5. ⏳ Добавить мониторинг метрик
6. ⏳ Интегрировать систему биллинга
7. ⏳ Развернуть на production сервере

---

**Дата создания:** 7 декабря 2025  
**Версия:** 1.0  
**Статус:** Production Ready ✅
