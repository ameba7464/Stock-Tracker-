# 🛠️ Руководство по Админ-Панели

## Обзор

Админ-панель позволяет управлять пользователями вашего Stock Tracker приложения. Вы можете просматривать информацию о пользователях, выдавать права на использование приложения, изменять тарифные планы и управлять доступом.

## 🚀 Быстрый старт

### 1. Запуск миграции базы данных

Сначала нужно применить миграцию для добавления поля `is_admin`:

```bash
# Перейдите в директорию проекта
cd Stock-Tracker

# Активируйте виртуальное окружение (если еще не активировано)
.venv\Scripts\Activate.ps1

# Примените миграцию
alembic upgrade head
```

### 2. Создание первого администратора

Чтобы получить доступ к админ-панели, вам нужно вручную установить флаг `is_admin` для вашего пользователя в базе данных:

```sql
-- Подключитесь к PostgreSQL и выполните:
UPDATE users 
SET is_admin = true 
WHERE email = 'ваш_email@example.com';
```

Или через Python:

```python
from stock_tracker.database.connection import SessionLocal
from stock_tracker.database.models import User

db = SessionLocal()
user = db.query(User).filter(User.email == "ваш_email@example.com").first()
user.is_admin = True
db.commit()
db.close()
```

### 3. Доступ к админ-панели

После запуска приложения, админ-панель доступна по адресу:

```
http://localhost:8000/admin/admin.html
```

## 📋 Функциональность

### Статистика

На главном экране вы увидите карточки со статистикой:
- **Всего пользователей** - общее количество зарегистрированных пользователей
- **Активных** - количество активных пользователей
- **Админов** - количество пользователей с правами администратора
- **Тенантов** - количество организаций (tenant)

### Таблица пользователей

Отображает список всех пользователей с информацией:
- Email
- Полное имя
- Название организации
- Тарифный план (Free, Starter, Pro, Enterprise)
- Статус (Активен/Неактивен)
- Роль (Admin/User)
- Дата регистрации

### Поиск и фильтрация

- **Поиск** - поиск по email, имени или названию организации
- **Фильтр по тарифу** - показать пользователей с конкретным тарифным планом
- **Только активные** - показать только активных пользователей

### Детальная информация о пользователе

Кликните на любого пользователя в таблице, чтобы открыть модальное окно с подробной информацией:

#### 👤 Информация о пользователе
- Email
- Полное имя
- Роль в системе
- Статус (активен/неактивен)
- Email подтвержден

#### 🏢 Организация
- Название организации
- Маркетплейс (Wildberries/Ozon)

#### 💳 Подписка
- Текущий тарифный план
- Статус подписки
- Использованная квота (запросы)

#### 📅 Активность
- Дата регистрации
- Последний вход в систему

### Управление пользователем

В модальном окне доступны следующие действия:

#### 1. Выдать/Забрать Admin
Дает или забирает права администратора. Администраторы имеют доступ к админ-панели.

#### 2. Активировать/Деактивировать
Включает или отключает доступ пользователя к приложению.

#### 3. Изменить тариф
Позволяет изменить тарифный план пользователя:
- **Free** - 100 запросов/час, 100 товаров
- **Starter** - 500 запросов/час, 500 товаров
- **Pro** - 1000 запросов/час, 5000 товаров
- **Enterprise** - без ограничений

#### 4. Деактивировать
Мягкое удаление - устанавливает `is_active = false`, но не удаляет пользователя из базы данных.

## 🔒 Безопасность

### Проверка прав доступа

Все эндпоинты админ-панели защищены middleware `require_admin`, который проверяет:
1. Наличие действительного JWT токена
2. Флаг `is_admin = true` у текущего пользователя

Если пользователь не является администратором, он получит ошибку `403 Forbidden`.

### Защита от самоудаления

Система предотвращает:
- Удаление администратором своего собственного аккаунта
- Снятие администратором с себя прав администратора

## 🎨 Кастомизация

### Изменение стилей

Все стили находятся в `<style>` секции файла `admin.html`. Вы можете изменить:
- Цветовую схему (переменные в `:root`)
- Размеры и отступы
- Шрифты
- Анимации

### Добавление новых полей

Чтобы добавить новое поле для отображения:

1. Добавьте поле в модель `User` (если его нет)
2. Обновите `UserDetailResponse` в `admin.py`
3. Обновите функцию `renderUserDetails()` в `admin.html`

## 📡 API Эндпоинты

### GET /api/v1/admin/stats
Получить статистику

**Ответ:**
```json
{
  "total_users": 150,
  "active_users": 142,
  "admin_users": 3,
  "total_tenants": 148,
  "free_plan_count": 100,
  "starter_plan_count": 30,
  "pro_plan_count": 15,
  "enterprise_plan_count": 3
}
```

### GET /api/v1/admin/users
Получить список пользователей (с пагинацией)

**Параметры:**
- `page` (int) - номер страницы (по умолчанию 1)
- `page_size` (int) - количество элементов на странице (по умолчанию 20, макс 100)
- `search` (str) - поисковый запрос
- `plan_filter` (str) - фильтр по тарифу
- `active_only` (bool) - показывать только активных

**Ответ:**
```json
{
  "users": [...],
  "total": 150,
  "page": 1,
  "page_size": 20,
  "total_pages": 8
}
```

### GET /api/v1/admin/users/{user_id}
Получить детальную информацию о пользователе

### PATCH /api/v1/admin/users/{user_id}/access
Обновить права доступа пользователя

**Тело запроса:**
```json
{
  "is_active": true,
  "is_admin": false,
  "plan_type": "pro"
}
```

### DELETE /api/v1/admin/users/{user_id}
Деактивировать пользователя (мягкое удаление)

## 🐛 Устранение неполадок

### Ошибка 403 при доступе к админ-панели

**Проблема:** Ваш пользователь не имеет прав администратора.

**Решение:** Установите флаг `is_admin = true` в базе данных для вашего пользователя.

### Админ-панель не загружается

**Проблема:** Статические файлы не найдены.

**Решение:** Убедитесь, что:
1. Файл `admin.html` находится в `src/stock_tracker/static/`
2. Путь к статическим файлам правильно настроен в `main.py`
3. Приложение перезапущено после добавления файлов

### Ошибка при загрузке списка пользователей

**Проблема:** API возвращает ошибку 500.

**Решение:**
1. Проверьте логи приложения
2. Убедитесь, что миграция применена
3. Проверьте соединение с базой данных

## 💡 Советы

1. **Используйте поиск** - быстро находите нужных пользователей по email или имени
2. **Фильтруйте по тарифу** - удобно управлять пользователями на конкретном тарифе
3. **Выдавайте доступ друзьям** - можете легко активировать аккаунты друзей и выдать им нужный тариф бесплатно
4. **Следите за квотами** - в деталях пользователя видно, сколько запросов использовано
5. **Регулярно проверяйте статистику** - помогает понять рост и использование приложения

## 🔄 Обновление

При обновлении админ-панели:

1. Сделайте резервную копию текущего файла
2. Обновите `admin.html` и/или `admin.py`
3. Если изменилась структура базы данных, создайте и примените миграцию
4. Протестируйте все функции перед развертыванием в production

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте логи приложения
2. Изучите документацию API
3. Проверьте правильность конфигурации базы данных
4. Убедитесь, что все зависимости установлены

## 🎯 Дальнейшее развитие

Идеи для улучшения админ-панели:

- [ ] Экспорт списка пользователей в CSV/Excel
- [ ] Массовые операции (выделить несколько пользователей и применить действие)
- [ ] Графики и аналитика использования
- [ ] История изменений (audit log)
- [ ] Отправка уведомлений пользователям
- [ ] Управление тарифами и ценами
- [ ] Двухфакторная аутентификация для админов
- [ ] Роли с гранулярными правами
