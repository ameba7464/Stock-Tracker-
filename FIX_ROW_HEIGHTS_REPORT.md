# Исправление смещения строк из-за длинных названий складов

**Дата:** 10 ноября 2025 г.  
**Проблема:** Длинные названия складов (например, "Екатеринбург - Перспективный 12") занимали 2 строки в ячейке, что смещало остальные данные и затрудняло отслеживание.

## ✅ Решение

### 1. Отключён перенос текста в колонке F (Название склада)
- **Было:** `wrap_text=True` 
- **Стало:** `wrap_text=False`
- **Результат:** Текст остаётся в одной строке и не переносится

### 2. Увеличена ширина колонки F
- **Было:** 180px
- **Стало:** 250px
- **Результат:** Больше места для длинных названий

### 3. Установлена фиксированная высота строк
- **Было:** Динамический расчёт высоты (25 * количество_строк + 10)
- **Стало:** Фиксированная высота 21px (стандарт Google Sheets)
- **Результат:** Все строки одинаковой высоты, данные не смещаются

## 📊 Изменённые файлы

### `Stock-Tracker/src/stock_tracker/database/structure.py`

#### Изменение 1: Настройки колонки F (строки 92-99)
```python
ColumnDefinition(
    key="warehouse_names",
    header="Название склада",
    letter="F",
    width=250,  # Увеличена ширина для длинных названий
    number_format="TEXT",
    alignment="LEFT",
    wrap_text=False  # ИСПРАВЛЕНО 10.11.2025: Отключён перенос текста
),
```

#### Изменение 2: Функция set_row_heights_for_multiline_data (строки 756-794)
```python
def set_row_heights_for_multiline_data(self, worksheet: gspread.Worksheet, 
                                     min_height: int = 21) -> None:
    """
    Set fixed row heights to prevent row expansion from long warehouse names.
    
    ИСПРАВЛЕНО 10.11.2025: Вместо динамического расчёта высоты строк,
    устанавливаем фиксированную высоту для всех строк данных.
    Это предотвращает смещение данных при длинных названиях складов.
    """
    # Устанавливаем фиксированную высоту 21px для всех строк данных
    # (стандартная высота Google Sheets)
```

## 🧪 Тестирование

Создан тест `test_fixed_row_heights.py`, который проверяет:
- ✅ `wrap_text=False` для колонки F
- ✅ Ширина колонки ≥ 250px
- ✅ Фиксированная высота строк 21px

**Результат:** Все проверки пройдены ✅

## 📱 Визуальный эффект

**До исправления:**
- Длинные названия складов переносились на 2 строки
- Строки становились высокими
- Данные в соседних колонках смещались вниз
- Сложно отслеживать данные по горизонтали

**После исправления:**
- Все названия складов в одной строке
- Все строки одинаковой фиксированной высоты
- Данные выровнены по горизонтали
- Удобное отслеживание по всем колонкам

## 🚀 Применение изменений

Изменения применяются автоматически при следующем запуске:
```bash
python update_table_fixed.py
```

Скрипт автоматически:
1. Создаст структуру таблицы с новыми настройками
2. Установит фиксированную высоту строк
3. Заполнит данные с правильным форматированием

---

**Примечание:** Названия складов не сокращаются - они просто остаются в одной строке. Если название слишком длинное и не помещается в колонку, оно будет обрезано визуально, но полное название можно увидеть при клике на ячейку.
